# 测试清单：热键自动激活游戏窗口（#213 失焦操作）

> 对应更改：实现 issue #213"根据鼠标下方的顶层窗口判断是否触发某些热键"——游戏窗口失焦时，鼠标悬停在游戏窗口上即可触发热键（无需先获取窗口焦点）。改动两处：① `HotkeyContext` 键盘键判定改为"游戏为活动窗口 OR 鼠标悬停游戏窗口"（纯判定无副作用）；② `_RegisterOne` 对鼠标键/键盘键主热键与 OnUp 型动作回调统一包装（`_WrapAction`）：执行前 `WinActivate` 游戏 + `WinWaitActive`（超时 500ms 则跳过动作）；激活后**不恢复原窗口**（焦点留在游戏，由用户自行切换回）。守卫补发 Up 变体与 SwitchKey 切换键不包装。

## 测试环境
| 项目 | 信息 |
|------|------|
| AFA 版本 | `v1.7.2` |
| AutoHotkey 版本 | `v2.0.26` |
| Windows 版本 | `Windows 11 10.0.26200` |
| 测试日期 | `2026-08-05` |

---

## 单元测试

### 功能点：热键注册无报错（新增闭包包装回调）

- [x] **操作**：启动 AFA（保持热键启用状态），查看日志
- [x] **预期**：无 `[ERROR] [Hotkey] 注册热键失败` 记录；`_WrapAction` 闭包回调被 `Hotkey` 正常接受（无 `Invalid callback function`），热键数量与配置一致

---

## 集成测试

### 流程 1：核心——游戏失焦 + 鼠标悬停游戏 + 鼠标键热键触发并生效

> 涉及模块：`hotkey_control.ahk`、`hotkey_actions.ahk`

- [x] **前置**：游戏窗口化运行（可见、非最小化）；**另一个窗口在前台**（如浏览器/记事本，模拟"主屏看录播"）；AFA 热键启用；某鼠标键（如侧键 XButton2）绑定"发送技能"，另一鼠标键（如侧键 XButton1）绑定"撤退"
- [x] **操作**：鼠标移到游戏窗口上（游戏窗口可见但**不激活**，前台是浏览器），按侧键 XButton2
- [x] **预期**：AFA 功能触发（游戏内技能生效）；**焦点切到游戏窗口并保留**；浏览器未收到任何误按键
- [x] **操作**：鼠标移到浏览器窗口（离开游戏），按侧键 XButton2
- [x] **预期**：不触发 AFA 功能，不激活游戏，点击正常（窗口外判定回归）

### 流程 2：核心——游戏失焦 + 鼠标悬停游戏 + 键盘热键触发

- [x] **前置**：键盘热键（如撤退=q、按下暂停=f）绑定；游戏失焦、鼠标悬停游戏
- [x] **操作**：鼠标悬停游戏窗口，按 q
- [x] **预期**：撤退触发（游戏激活期间动作执行），焦点留在游戏；**q 不落到浏览器**（被 AFA 拦截）
- [x] **操作**：鼠标移到浏览器上，按 q
- [x] **预期**：q 正常输入浏览器（游戏外打字回归）

### 流程 3：焦点留在游戏（不自动恢复）

> 涉及模块：`hotkey_control.ahk`（`_WrapAction` 激活后不恢复）

- [x] **操作**：触发一次热键，观察动作结束后活动窗口
- [x] **预期**：焦点留在游戏窗口（不自动切回浏览器），功能完整执行
- [x] **操作**：触发后手动切回浏览器
- [x] **预期**：手动切换正常（焦点由用户自行管理）

### 流程 4：游戏已激活时无回归（包装 no-op）

- [x] **操作**：游戏为活动窗口（正常游玩状态），鼠标悬停游戏，按鼠标键/键盘热键
- [x] **预期**：各功能正常触发；焦点无变化、无抖动（`WinActivate`/`WinWaitActive` 对已激活窗口是 no-op）
- [x] **操作**：连续快速触发多次（如连按撤退 5 次）
- [x] **预期**：每次功能正常，焦点稳定在游戏，无累积抖动

### 流程 5：含延迟动作（过帧）在失焦悬停时执行

- [x] **前置**：过帧热键（16ms/33ms/166ms）绑定
- [x] **操作**：游戏失焦 + 鼠标悬停游戏，按过帧热键
- [x] **预期**：过帧动作正常执行（激活游戏 → ESC+空格序列）；焦点留在游戏

### 流程 6：守卫 Up 变体不激活（回归 #257 失焦边界）

- [x] **前置**：`GameSpeed=<SHIFT` 配置；游戏内倍速绑左 Shift
- [x] **操作**：游戏内按住左 Shift，Alt+Tab 切走（Shift 仍按住），在其它窗口松开 Shift，切回游戏
- [x] **预期**：Shift 不卡（Up 变体补发 up）；**补发 up 时不激活游戏窗口**（焦点保持在切走的窗口）
- [x] **操作**：查看日志
- [x] **预期**：无 `[ERROR]`；无因包装引入的异常

### 流程 7：SwitchKey 切换键不包装

- [x] **前置**：配置了"启用/禁用热键"快捷键（如 F8）
- [x] **操作**：游戏失焦 + 鼠标悬停游戏，按切换键
- [x] **预期**：热键禁用/启用正常；切换键**不触发激活/恢复**（走原逻辑，焦点不变）

---

## 回归测试

### 功能：窗口外鼠标键不触发（#257 核心）

- [x] 验证：鼠标移到任务栏/桌面/其它窗口，绑定鼠标键（如右键）→ 不触发、点击正常透传（右键菜单正常弹出）

### 功能：游戏外打字正常（#257 关键成果）

- [x] 验证：鼠标在浏览器/记事本（不在游戏上），键盘输入 a/w/q/e/f 等 AFA 绑定键 → 正常打出字符，不被吞

### 功能：失焦边界与鼠标拖出（#257）

- [x] 验证：按住 Shift 切走松开不卡；游戏内按住右键拖出窗口松手不卡

### 功能：拦截日志去重（#257）

- [x] 验证：关卡外按住 w 切走，拦截日志只记一次（无刷屏）

### 功能：热键禁用/启用后重新注册

- [x] 验证：托盘禁用再启用热键后，包装仍生效（失焦悬停游戏能触发）、窗口外仍不触发

---

## 测试结果

- [x] 全部通过
- [ ] 存在问题（详见下方问题反馈）

## 问题反馈

本次测试未发现问题。

---

## 追加测试

> 对应更改：采纳审查建议——① `_WrapAction` 增加 `WinExist` 前置检查（游戏窗口不存在则跳过，避免阻塞热键线程）；② 设置开关类动作（`BeginPause`/`AutoBeginPauseSwitch`）加 `NoActivate` 标志，注册时不走焦点切换包装

### 流程 8：设置开关不抢焦点（NoActivate）

- [x] **操作**：游戏失焦 + 鼠标悬停游戏，按 AutoBeginPauseSwitch（c）
- [x] **预期**：切换"开局自动暂停"正常（托盘提示）；焦点保持不变（不激活游戏窗口）

### 流程 9：WinExist 防御（游戏窗口不存在）

- [x] **操作**：关闭游戏，按任意热键
- [x] **预期**：不触发、不激活、无 500ms 阻塞（`WinExist` 前置检查跳过）

### 流程 10：常规热键回归（#213 核心不受影响）

- [x] **操作**：游戏失焦 + 悬停游戏，按侧键/撤退
- [x] **预期**：功能生效、激活游戏、焦点留游戏

---

## 追加测试（失焦悬停操作开关）

> 对应更改：新增「自定义」页复选框「游戏窗口未激活时允许鼠标悬停在窗口上触发热键」，用于开启/关闭 #213 失焦悬停操作功能的**键盘键**路径——`HotkeyContext` 键盘键的 `IsMouseInClient()` 兜底由 `State.HoverOperate` 门控（保存/应用后经 `Loader.LoadSettings` 从 INI 同步）；**鼠标键/滚轮路径不变**，仍只受悬停 gate 约束。

## 测试环境（本轮）

| 项目 | 信息 |
|------|------|
| AFA 版本 | `v1.9.0` |
| AutoHotkey 版本 | `v2.0.26` |
| Windows 版本 | `Windows 11 10.0.26200` |
| 测试日期 | `2026-08-07` |

---

### 单元测试

#### 功能点：复选框控件

- [x] **操作**：打开设置窗口，切换到"其他设置"→ 左侧导航"自定义"
- [x] **预期**：分类中出现复选框「 游戏窗口未激活时允许鼠标悬停在窗口上触发热键」，单行显示不换行、无截断；位于"过帧档位3"下方、左列宽度（w290），不与右侧"顶部标签页"管理器重叠
- [x] **操作**：查看复选框初始勾选状态
- [x] **预期**：默认**已勾选**（功能默认开启，保持现有行为）

#### 功能点：勾选状态与按钮联动

- [x] **操作**：取消勾选复选框
- [x] **预期**："保存并关闭"、"应用设置"按钮变为可用；底部出现"修改尚未保存或应用"提示
- [x] **操作**：重新勾选（恢复原状）
- [x] **预期**：按钮恢复禁用状态（修改状态消除）
- [x] **操作**：取消勾选后点击"取消"
- [x] **预期**：复选框恢复为勾选状态（修改被丢弃，未保存不生效）

#### 功能点：配置持久化

- [x] **操作**：取消勾选并保存，检查 `%AppData%\ArknightsFrameAssistant\PC\Settings.ini`
- [x] **预期**：`[Custom]` 下 `HoverOperate=0`
- [x] **操作**：重新勾选并保存，再次检查
- [x] **预期**：`[Custom]` 下 `HoverOperate=1`
- [x] **操作**：删除 Settings.ini 中 `HoverOperate` 键后重启 AFA，再检查 INI
- [x] **预期**：无该键时回退默认开启且**自动补齐**——重启后 `[Custom]` 下 `HoverOperate=1` 被重新写回（`_BackfillMissingCustomDefaults` 迁移），复选框为勾选

---

### 集成测试

#### 流程 1：开启时失焦悬停操作正常（#213 核心回归）

> 涉及模块：`hotkey_control.ahk`、`hotkey_actions.ahk`、`gui.ahk`

- [x] **前置**：复选框为勾选状态并保存；游戏窗口化运行；另一窗口在前台；键盘热键（如撤退=q）已绑定
- [x] **操作**：游戏失焦、鼠标悬停游戏窗口，按 q
- [x] **预期**：撤退触发、焦点切到游戏并保留（#213 原行为不受影响）

#### 流程 2：关闭后键盘键失焦悬停不再触发

> 涉及模块：`hotkey_control.ahk`（`HotkeyContext`）

- [x] **前置**：取消勾选复选框并保存（`HoverOperate=0`）；游戏失焦、鼠标悬停游戏窗口
- [x] **操作**：按 q（或其它键盘热键）
- [x] **预期**：AFA 功能**不触发**、游戏不激活；q 落到当前前台窗口（透传）
- [x] **操作**：鼠标移到浏览器（不在游戏上），按 q
- [x] **预期**：q 正常输入浏览器（游戏外打字回归不受影响）

#### 流程 3：开关不影响鼠标键路径

> 涉及模块：`hotkey_control.ahk`、`hotkey_actions.ahk`

- [x] **前置**：开关仍为关闭并保存；某鼠标键（如侧键 XButton2）绑定"发送技能"
- [x] **操作**：游戏失焦、鼠标悬停游戏窗口，按侧键 XButton2
- [x] **预期**：功能**仍触发**（鼠标键仅受悬停 gate 约束，不受本开关控制）
- [x] **操作**：鼠标移到浏览器/任务栏（离开游戏窗口），按侧键
- [x] **预期**：不触发、点击正常透传（#257 鼠标 gate 回归）

#### 流程 4：开关保存/应用后生效（勾选不立即生效）

> 涉及模块：`gui.ahk`、`settings/loader.ahk`

- [x] **前置**：开关当前为开启；取消勾选复选框但**不保存**，保持设置窗口打开
- [x] **操作**：游戏失焦、鼠标悬停游戏，按键盘热键 q
- [x] **预期**：功能**仍触发**（未保存时 `State.HoverOperate` 仍为旧值，符合"保存/应用后生效"）
- [x] **操作**：点击"应用设置"，再次按 q
- [x] **预期**：功能**不再触发**（保存/应用后开关即时生效）

#### 流程 5：重新开启恢复功能

- [x] **前置**：开关为关闭
- [x] **操作**：重新勾选并保存；游戏失焦、鼠标悬停游戏，按键盘热键 q
- [x] **预期**：功能恢复触发、激活游戏（开关重新开启生效）

#### 流程 6：游戏已激活时开关无影响

- [x] **前置**：开关关闭并保存；游戏为活动窗口（正常游玩）
- [x] **操作**：鼠标悬停游戏，按键盘/鼠标键热键
- [x] **预期**：功能正常触发、焦点无变化（游戏激活时判定不受开关影响）

---

### 回归测试

#### 功能：失焦边界与卡键（#257）

- [x] 验证：按住修饰键（如左 Shift）Alt+Tab 切走再松开，不卡键（Up 变体补发逻辑不受开关影响）

#### 功能：热键禁用/启用后注册正常

- [x] 验证：托盘"启用/禁用热键"切换后，开关状态保持、失焦悬停行为与保存的设置一致

#### 功能：设置取消/重置流程

- [x] 验证：自定义页勾选/取消勾选后点"取消"，复选框状态恢复且热键行为不变

---

## 测试结果（本轮追加）

- [x] 全部通过
- [ ] 存在问题（详见下方问题反馈）

## 问题反馈（本轮追加）

本轮测试未发现问题。
