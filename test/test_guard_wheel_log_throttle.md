# 测试清单：`guard_wheel_log_throttle`

> 对应更改：`守卫拦截日志按 100ms 时间窗节流（KeyForward.ShouldLogGuard）——滚轮无 down/up 状态不写 InterceptedKeys、每次独立滚动都走拦截路径，无极/高分辨率滚轮洪峰下若逐条 Info 落盘会形成"每档位一次文件 IO"并刷爆日志轨；本次将滚轮拦截日志按 100ms 时间窗去重（普通键维持 InterceptedKeys 原去重语义）`

## 测试环境

| 项目 | 信息 |
|------|------|
| AFA 版本 | `v2.0.1` |
| AutoHotkey 版本 | `v2.0.26` |
| Windows 版本 | `Windows 11 25H2` |
| 测试日期 | `2026-08-28` |

---

## 单元测试

### 功能点：滚轮守卫拦截日志按 100ms 时间窗节流

- [ ] **操作**：设置中确认「仅在关卡内启用常规作战热键」（关卡守卫）为开启状态；将任一常规作战功能（如「技能」）绑定为滚轮键（WheelDown），保存/应用（热键重建后生效）。
- [ ] **预期**：绑定保存无异常，无警告弹窗。
- [ ] **操作**：启动 AFA、进入游戏并停留在**非关卡界面**（如主界面/基建），将鼠标悬停在游戏窗口上，用鼠标滚轮快速连续滚动 ≥2 秒后停止。
- [ ] **预期**：打开 `%AppData%\ArknightsFrameAssistant\PC\logs\` 下本次会话的 `afa-*.log`，「被关卡检测拦截（不在关卡界面）」条目仅在滚动期间出现，且**相邻条目时间戳间隔 ≥100ms 左右**（2 秒内最多约 20 条），不再是每档位一条的刷屏（日志行带毫秒时间戳可直接核对）。
- [ ] **操作**：（如有条件）使用无极（高分辨率/自由滚动）滚轮重复上述快速滚动 ≥2 秒。（跳过：测试机无无极滚轮，无法验证高事件速率场景——待报告人/有条件环境实测）
- [ ] **预期**：日志仍按 ≤10 条/秒 节流；全程无系统"嘟嘟"声、无 AHK 警告弹窗（验证本次修复目标）。（跳过：同上）

### 功能点：普通按键拦截日志去重语义不变

- [ ] **操作**：保持守卫开启，将任一常规作战功能绑定为普通键（如 F），在非关卡界面**按住 F 约 2 秒再松开**（触发 key repeat）。
- [ ] **预期**：按住期间拦截日志只有**一条**（InterceptedKeys 去重语义与改动前完全一致，未被时间窗节流影响）。
- [ ] **操作**：快速连按 F ≥10 次（每次独立按下/松开）。
- [ ] **预期**：每次按下周期各记录一条拦截日志（共约 10 条），无遗漏、无多余。

---

## 集成测试

### 流程：守卫开启 + 关卡外快速滚动滚轮（完整链路）

> 涉及模块：`LevelDetector`、`GuardInLevel`、`KeyForward`、`Logger`

- [ ] **前置**：关卡守卫开启；常规作战功能绑定滚轮键；AFA 运行、游戏在前台、处于非关卡界面；鼠标悬停在游戏窗口上。
- [ ] **操作**：快速滚动滚轮 ≥2 秒。
- [ ] **预期**：滚轮在游戏内正常透传（游戏列表正常滚动、无"滚动距离翻倍"）；绑定的常规作战功能**不被触发**（如未发送技能键）；拦截日志 ≈≤10 条/秒；无系统"嘟嘟"声/警告弹窗。
- [ ] **异常**：快速滚动滚轮的同时，按下另一个绑定常规作战功能的普通键（如 s）。
- [ ] **预期**：该普通键功能正常执行（节流只影响拦截**日志写入**，不影响拦截/透传/功能逻辑）。

### 流程：守卫开启 + 关卡内滚轮热键放行路径无回归

> 涉及模块：`LevelDetector`、`GuardInLevel`

- [ ] **前置**：进入关卡（关卡检测判定 `InLevel=true`）。
- [ ] **操作**：触发一次绑定的滚轮热键功能。
- [ ] **预期**：功能正常执行（如发送一次技能键）；关卡内**无**拦截日志（放行路径不走拦截日志分支）。

### 流程：守卫关闭 + 滚轮热键无回归

> 涉及模块：`LevelDetector`、`GuardInLevel`、`HotkeyActions`

- [ ] **前置**：在设置中关闭「仅在关卡内启用常规作战热键」并应用。
- [ ] **操作**：触发滚轮绑定功能。
- [ ] **预期**：功能照常执行（守卫关闭时 `InLevel` 强制 true 直接放行）；日志无拦截条目，与改动前行为一致。

### 流程：热键重建后节流状态正常

> 涉及模块：`HotkeyService`、`KeyForward`

- [ ] **前置**：守卫开启、滚轮已绑定，且刚触发过一节流窗口内的拦截日志。
- [ ] **操作**：立即保存一次设置（触发 `EnableByTab` 热键重建），随后再次滚动滚轮触发拦截。
- [ ] **预期**：重建后的第一条拦截日志正常记录（节流是自然滑动窗口、无残留状态导致日志长时间丢失）。

---

## 回归测试

### 功能：滚轮透传语义

- [ ] 验证：关卡外滚动滚轮，游戏内滚动距离**不翻倍**（`ForwardOriginalKey` 对带 `~` 前缀的滚轮不补发，本次改动未触碰该逻辑）。

### 功能：过帧时序

- [ ] 验证：16/33/166ms 过帧三件套在关卡内节奏正常（未触碰时序敏感路径，仅确认无回归）。

### 功能：日志系统

- [ ] 验证：快速滚动 2 秒后，普通日志文件增量远小于此前的"每档位一条"量级；日志轮换/清理无异常。

---

## 测试结果

> 注：2026-08-28 用户暂缓本次手工验证（测试机无无极滚轮；无级滚轮相关项已标注跳过）。其余项（普通滚轮、普通键、流程类）待后续验证，当前保持未测状态。

- [ ] 全部通过
- [ ] 存在问题（详见下方问题反馈）

## 问题反馈

### 问题1：[问题描述]
- [ ] 已解决
