<!-- 可交由AI根据此模版生成测试清单以节约工作量 -->
# 测试清单：`decoupling_phase5`

> 对应更改：阶段 5「热键域重构」——`game_keys`/`hotkey_service`/`hotkey_actions` 移入 `core/hotkey/`，`HotkeyController` 更名 `HotkeyService`，`Action*` 类化为 `HotkeyActions`，`GameKeys` 改为发布 `GameKeysChanged`，热键禁用后注册表变更不再恢复热键，GUI 改为订阅热键事实事件。

## 测试环境
| 项目 | 信息 |
|------|------|
| AutoHotkey 版本 | v2.0.26 |
| Windows 版本 | 待确认 |
| 测试日期 | 2026-08-18 |

---

## 单元测试

### 功能点：热键总开关与注册表变更（目标 bug 修复）

- [x] **操作**：启动 AFA，使用热键总开关禁用热键
- [x] **预期**：热键被禁用，托盘提示「热键已禁用」
- [x] **操作**：在游戏设置中修改一个按键绑定（触发注册表变更）
- [x] **预期**：热键仍保持禁用，不会被注册表变更重新启用（修复目标 bug）
- [x] **操作**：重新启用热键
- [x] **预期**：热键按当前游戏按键绑定正常重建

### 功能点：标签页切换与热键组

- [x] **操作**：在设置窗口切换「常规作战」/「快捷操作」/「卫戍协议」标签
- [x] **预期**：热键组随标签切换正确启用；卫戍协议切换时托盘提示「已启用/已退出卫戍协议方案」
- [x] **操作**：热键禁用状态下切换标签页
- [x] **预期**：只记录当前标签，不重建热键；重新启用后按当前标签启用

### 功能点：切换键与托盘菜单

- [x] **操作**：设置/修改「启用/禁用热键快捷键」并应用
- [x] **预期**：托盘菜单项显示对应按键文本（如 `启用/禁用热键(F1)`）
- [x] **操作**：使用切换键禁用/启用热键
- [x] **预期**：热键状态与托盘提示正确

### 功能点：开局自动暂停开关（SettingsValueChangeRequested 临时处理器）

- [x] **操作**：使用「开局自动暂停开关」热键
- [x] **预期**：设置项正确切换，配置保存，热键重建，托盘提示「已开启/已关闭开局自动暂停」

---

## 集成测试

### 流程：程序启动与热键域 include

> 涉及模块：`core/hotkey/game_keys.ahk`、`core/hotkey/hotkey_service.ahk`、`core/hotkey/hotkey_actions.ahk`

- [x] **前置**：无游戏进程或游戏未启动均可
- [x] **操作**：启动 AFA
- [x] **预期**：正常启动，无类未定义/回调绑定报错；热键注册正常
- [x] **操作**：运行 `test/scripts/smoke_test.ahk`
- [x] **预期**：退出码为 0

### 流程：热键动作与守卫

> 涉及模块：`HotkeyActions`、`LevelDetector`、`GameMonitor`

- [x] **前置**：明日方舟游戏窗口已打开并进入关卡
- [x] **操作**：使用常规作战/快捷操作/卫戍协议热键
- [x] **预期**：动作正常触发，关卡守卫正常
- [x] **操作**：使用过帧 16/33/166ms 热键
- [x] **预期**：过帧时序正常

### 流程：设置保存/应用/取消/重置

> 涉及模块：`settings/actions.ahk`、`HotkeyService`

- [x] **操作**：修改热键并保存/应用
- [x] **预期**：热键按当前标签正确重建
- [x] **操作**：重置按键
- [x] **预期**：热键恢复默认并重建

---

## 回归测试

### 功能：原有热键行为

- [x] 验证：三组热键互斥启用、热键总开关正常
- [x] 验证：鼠标/滚轮/键盘录制正常
- [x] 验证：更新检查/公告正常
- [x] 验证：自动开局暂停与自动退出正常

### 功能：静态检查

- [x] 验证：`grep -n "GuiManager" src/lib/core/hotkey/*.ahk` 无代码引用（注释除外）
- [x] 验证：`python3 tools/layer_check.py --baseline KNOWN_VIOLATIONS` 输出 `PASS`
- [x] 验证：基线中不再包含 `game_keys -> GuiManager`、`hotkey_service -> GuiManager` 违规

---

## 测试结果

- [x] 全部通过
- [ ] 存在问题（详见下方问题反馈）

## 问题反馈

### 问题1：[问题描述]
- [ ] 已解决

### 问题2：[问题描述]
- [ ] 已解决
