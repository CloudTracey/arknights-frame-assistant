# 测试清单：`输入链诊断与脱敏修复`

> 对应更改：修复诊断包脱敏摧毁日志（移除不可靠的 USER 全文替换）、修复 noActivate 热键被 HookHealth 误判为「钩子被摘除」而触发无谓自愈；新增输入链耗时遥测（`ctxEval`/`inToAction`）与 HookHealth 快照扩展、动作关键阶段日志（带 500ms 节流）、自愈策略收紧（阈值 3→5、冷却 5s→30s、WARN 明示抢占优先级）、诊断包补环境事实（外设/宏软件进程）。

## 测试环境

| 项目 | 信息 |
|------|------|
| AFA 版本 | `v2.1.0` |
| AutoHotkey 版本 | `v2.0.26` |
| Windows 版本 | `Windows 11 家庭中文版 10.0.26200` |
| 测试日期 | `2026-09-20` |

---

## 单元测试

### 功能点：诊断包脱敏不再摧毁日志

- [x] **操作**：设置里点「生成日志压缩包」，导出一份 zip 到桌面
- [x] **预期**：解压后打开 `logs\` 下的日志，时间戳格式完整可读（形如 `2026-09-20 11:46:16.123`），不出现 `<USER>` 占位符
- [x] **操作**：在日志里搜 `pid=`、`handle=`、`0x`、纯数字计数
- [x] **预期**：数字与句柄完整，没有被替换成占位符（本次修复前用户名短时这些都会被大面积破坏）
- [x] **操作**：解压后打开 `settings-sanitized.ini`
- [x] **预期**：`GitHubToken=<redacted>` 与 `GitHubTokenProtected=<redacted>` 两行仍在；`GamePath=` 等路径里的 `C:\Users\<你的用户名>` 部分被替换为 `<USERPROFILE>`
- [ ] **操作**：导出成功后查看弹窗
- [ ] **预期**：弹窗正文只有「日志压缩包已生成」，**不再回显完整路径**（路径是用户自己选的，且非标准位置的用户目录无法被 `<USERPROFILE>` 遮盖）

### 功能点：HookHealth 快照新增字段

- [x] **操作**：正常启动 AFA 并等待约 1 分钟（心跳快照间隔 60s），在日志中搜 `[HookHealth] 心跳`
- [x] **预期**：快照里出现 `recover=` 与 `ctxEval=…/…ms(max/avg, n=…)`，且 `ctxEval` 的 `n` 随按键增长（没按键时为 0）
  > 实测（2026-09-21 15:49 会话）：字段齐全；`ctxEval` 峰值 5.7ms / 均值 0.3ms (n=270)，完全正常。
- [x] ~~**操作**：进游戏按几次普通热键，观察 `inToAction` 的 max/avg~~
  > **此项已作废**：`inToAction` 指标整项移除（详见下方「追加测试 / 度量缺陷」的最终结论）。
- [x] **操作**：观察 `fire=` 计数与按键次数
- [x] **预期**：`fire` 可能大于按键次数（守卫键的 Up 变体补发也算一次回调），这是预期行为，不算异常

### 功能点：（已回滚，勿据此判读）动作阶段日志与节流

> **本节整组作废**：动作内的阶段日志实现后又被整体回滚——在 `Send "{X Down}"` 与 `Send "{X Up}"` 之间插入日志 I/O，会把这段间隙从"远小于游戏一帧"拉长到跨帧，使同一操作时灵时不灵（实测：右键「返回上级菜单」+勾选放弃行动时，时而返回、时而放弃行动）。原因与替代做法见 [docs/design/input_stall_diagnosis.md](../../docs/design/input_stall_diagnosis.md)。**当前动作代码中不存在阶段日志**，下列各条已不再是有效验证项，保留仅为记录。

### 功能点：自愈策略参数

- [x] **操作**：（静态确认，无需运行）查看 `src/lib/core/diagnostics/hook_health.ahk` 顶部参数
- [x] **预期**：`MissThreshold = 5`、`RecoverCooldownMs = 30000`，且自愈前会先落一条含「已强制重装键盘钩子并抢占优先级」的 WARN

---

## 集成测试

### 流程：导出诊断包并核对环境事实

> 涉及模块：`log_exporter.ahk`、`logger.ahk`

- [x] **操作**：导出诊断包，打开 `diagnostics.txt`
- [x] **预期 A**：出现 `RunningProcessCount=N` 与 `RunningProcesses=<全部进程名>`——**完整列表**、按字母排序、空格分隔、只列名字（无 PID、无路径、无命令行）
- [x] **预期 B**：列表里能找到 AFA 自己的进程名（`main.exe` 或 `AutoHotkey64.exe`），以及你认识的外设软件（如 `INZONEHub.exe`、`GCUService.exe`、`logi_lamparray_service.AMD64.exe`）
- [x] **操作**：确认日志里**没有** `InputSoftware.` 与 `InputSoftwareKeywordMatches` 这类派生行
- [x] **预期 C**：一行都没有——只保留完整列表（清单/关键词匹配已移除，避免给出会漏的索引）
- [x] **操作**：确认只记录事实
- [x] **预期 D**：`RunningProcesses` 不含路径、不含用户名、不含任何"疑似导致卡顿"之类的结论文字

### 流程：noActivate 热键不再被误报为「钩子失效」（本次修复核心）

> 涉及模块：`hotkey_service.ahk`、`hook_health.ahk`、`hotkey_actions.ahk`

- [x] **前置**：把「开局自动暂停开关」与「开局自动二倍速开关」各绑一个键（例如 `c` / `b`），启动 AFA 并让游戏在前台
- [x] **操作**：长按其中一个键 5 秒以上，再松开；重复 3 次（按住不放期间动作会被 `PureKeyWait` 挂住，属正常）
- [x] **预期**：日志里**不出现** `[WARN] [HookHealth] 物理按下未触发热键`，也不出现 `键盘钩子疑似失效` 与任何「钩子自愈」WARN
- [x] **操作**：检查日志中 `[HookHealth]` 快照的 `fire=` 计数
- [x] **预期**：每按一次该键 `fire` 至少 +1（修复前该路径完全不计数）
- [x] **操作**：确认功能本身没变
- [x] **预期**：按键能正常切换开/关状态；若此时游戏不在前台但鼠标悬停在游戏窗口上，按这个键**不会**把游戏窗口激活到前台（noActivate 语义保持，不引入 WinActivate）

### 流程：耗时与探针观测的判读（已按最终实现收敛）

> 原"输入链耗时遥测"里的 `inToAction` 已整项移除（测不准），本组只保留仍成立的两件事：`ctxEval` 超标告警、以及探针自证计数的读法。

- [x] **前置**：正常游玩若干分钟，期间正常使用各类热键
- [x] **操作**：在日志里搜 `HotIf 求值耗时异常`
- [x] **预期**：正常情况下**不出现**（阈值 50ms）；若出现，该行带本次耗时、峰值、累计次数——这是**判定路径被拖**的直接证据
- [x] **操作**：任取一条 `[HookHealth] 心跳` 快照，看 `miss=`、`idle/idleKbd/idlePhys` 三值与 `probe=[…]`
- [x] **预期**：三值有差异（说明钩子仍在区分键盘/鼠标输入）；`probe` 里各键满足 `arm ≈ cleared + miss + discard`，`miss` 不增长

##### 异常路径

- [x] **异常**：游戏窗口关闭后再按热键 → **预期**：日志出现「动作跳过：目标游戏窗口不存在」，无异常弹窗，`depth` 回落不残留
- [x] **异常**：游戏失焦且鼠标不在游戏窗口内按热键 → **预期**：热键不触发（判定层拦截），不产生阶段日志，也不影响前台程序正常接收按键

---

## 回归测试

### 功能：热键动作本身无行为变化

- [x] 验证：常规作战热键（暂停 `f`、倍速 `d`、技能 `s`、撤退 `a`、过帧 `r`/`t`）行为与修复前一致
- [x] 验证：失焦悬停操作仍然可用（鼠标悬停在游戏窗口上即可操作，且执行动作前会激活游戏窗口）
- [x] 验证：关卡守卫仍生效（不在关卡时按键透传给游戏，不执行 AFA 动作）
- [x] 验证：守卫键的 Up 变体补发正常（按住 `f` 从游戏内拖出窗口再松开，不会卡键）

### 功能：模拟点击类动作的副作用清理

- [x] 验证：侧键类动作执行期间鼠标被临时 BlockInput，动作结束后鼠标立刻恢复可动（即使动作中途出错也不会一直锁住）
- [x] 验证：触控注入类动作（暂停选中）执行后光标回到原位置
- [x] 验证：「模拟左键点击」在按下期间游戏能持续收到按住状态，松开后正常抬起

### 功能：全局开关与日志系统

- [x] 验证：「启用/禁用热键」快捷键（默认 `<^MButton`）仍能正常切换热键开关
- [x] 验证：调试控制台开关仍能正常开/关，实时日志显示正常
- [x] 验证：日志轮转与容量清理仍正常（长时间运行后普通日志不超过预算）

---

## 追加测试

> 对应更改一：第 1 组测试中用户发现的优化点——导出成功弹窗原先回显完整 zip 路径，而该路径不经 `Logger.Redact`，非标准位置的用户目录（如 `D:\Importent\Users\<用户名>\Desktop`）无法被 `<USERPROFILE>` 遮盖。

### 功能点：导出成功弹窗不再回显路径

- [x] **操作**：导出一次日志压缩包
- [x] **预期**：弹窗标题「导出成功」，正文只有「日志压缩包已生成」，无任何路径
- [x] **操作**：把界面语言切到 English / 日本語 / 한국어 / 繁體中文，各导出一次
- [x] **预期**：四种语言下正文都不含路径、不含多余的「：」或空行（对应 locale 值已同步去掉 `{1}` 占位符）
- [x] **操作**：导出后查日志里的 `[Diagnostics] 日志压缩包已生成`
- [x] **预期**：该行仍只记文件名（不含目录），便于定位文件又不泄漏路径

> 对应更改二（**最终结论：整项移除**）：第 2 组测试中用户对 `inToAction` 数值存疑，三轮追查确认它**在生产上无法自证正确**——区间起点会被 key repeat 重复登记、清理动作一度被"游戏前台"门挡住，导致连报"输入交付 3.9s"假延迟；同一份数据里真排队与残留无法分辨。按"**测不准的指标一律不留**"原则，`inToAction` 及其分位数/`lastAct`/起点来源等字段已全部移除。

### 功能点：快照只保留可验证指标

- [x] **操作**：进游戏正常按几次热键，等一条 `[HookHealth] 心跳` 快照
- [x] **预期**：快照里**不再出现** `inToAction=` / `lastAct=`；仍有 `recover=` 与 `ctxEval=…/…ms(max/avg, n=…)`，且 `ctxEval` 的 `n` 随按键增长
- [x] **操作**：按住 `q`（一键撤退）约 1 秒后松开，1 秒内再按一次 `q`（原假延迟的复现步骤）
- [x] **预期**：两次动作阶段 1/4~4/4 都完整；日志里**不再出现** `输入交付→动作开始耗时异常`（该 WARN 已随指标移除）
- [x] **操作**：日志里搜 `输入交付→动作开始耗时异常`
- [x] **预期**：一条都没有（整项已移除）；若出现 `HotIf 求值耗时异常`，那才是需要关注的

> 对应更改三：按用户要求把阶段日志覆盖面从 2 条路径扩到全部"多阶段阻塞"动作（含共用实现消除重复），并给 `PureKeyWait` 加长按上限提示。

### 功能点：（已作废）阶段日志覆盖全部多阶段动作

> **整组作废，相关改动已回滚**：该方案在 `Send "{X Down}"` 与 `Send "{X Up}"` 之间插入了日志 I/O，把间隙从"远小于一帧"拉长到跨帧，导致游戏帧头轮询时而看得到、时而看不到该键。实测回归：把「返回上级菜单」绑到右键并勾选"使用返回上级菜单放弃行动"后，按右键时而在返回、时而在放弃行动，极不稳定。已整体回滚动作内的阶段打点与为它做的共用实现重构；**长按提示（`PureKeyWait`）保留**，它只在等待期间打点、不插在按键序列里。原因已写入 [docs/design/input_stall_diagnosis.md](../../docs/design/input_stall_diagnosis.md)。

### 功能点：长按上限提示（保留项）

- [x] **操作**：按住某个"按住型"热键（如 `f` 按下时暂停）超过 30 秒再松开
  > 实测（17:36:42）：出现一条 `[WARN] 等待物理松开已 30.0s（第 1 次提示）：key=f`，结尾不再有 `——对照…见 docs/…md`
- [x] **预期**：日志出现该 WARN；同一次按住不重复刷屏（60s 间隔 + 每周期上限），实测未出现「第 2 次提示」
- [x] **操作**：正常长按 5~10 秒
  > 实测（17:36:15 / 17:36:50 / 17:36:55 三次）：只有 `[DEBUG] 等待物理松开已 3.0s`，无 WARN
- [x] **预期**：只有 DEBUG 的 3s 提示，**不出现** WARN（门槛 30s，不会误伤正常长按）

> 对应更改四（**已随指标移除而作废**）：用户实测「按住 q 再按一次 q 会报一条 `输入交付→动作开始耗时异常` WARN，但按键功能完全正常」。该 WARN 属于 `inToAction` 指标（已整项移除），因此本组无需验证——保留此段仅为记录该现象的结论。

> 对应更改五：用户实测发现「用 XButton 触发功能后切换窗口，会多出一条意外的 WARN」。排查确认是未命中判定的**前提只检查了一次**（建档时），切窗后前提失效却仍按"没回调"记未命中 → 假阳性。已在结算时复核前提（前台窗口是否变化、鼠标键的光标是否仍在客户区）。

### 功能点：（已作废）切窗不再产生「未触发热键」假阳性

> **整组作废**：这一组基于"切窗导致假阳性"的错误结论，相关修复已回滚（实测证明不切窗同样复现，真因见下一组的"快速点按 + 漏掉抬起"）。保留标题仅为记录排查过程，无需验证。

### 功能点：快速点按不再误报「未触发热键」

- [x] **操作**：进游戏，鼠标停在游戏窗口内、游戏在前台，**快速连点 `XButton2`（暂停技能）5 次以上**（每次一按即松）
- [x] **预期**：功能每次都正常生效；日志里**不出现** `[WARN] [HookHealth] 物理按下未触发热键：key=xbutton2`，也不出现 `键盘钩子疑似失效` 或任何「钩子自愈」WARN
- [x] **操作**：同样快速连点 `XButton1`（暂停撤退）、以及键盘键（如 `f`/`q`）各 5 次以上
- [x] **预期**：同样无上述 WARN（判据修复对所有键生效）
- [x] **操作**：看随后任意一条 `[HookHealth] 心跳` 快照里的 `probe=[…]`
- [x] **预期**：出现类似 `xbutton2(arm=N,raceSkip=N,cleared=N,miss=0,discard=0)`，且 **`arm ≈ cleared + miss + discard`**；`miss` 不增长
- [ ] **操作**：（可选，验证检测能力没被削弱）用鼠标右键菜单或任务管理器把 `AutoHotkey64.exe` 挂起 10 秒后恢复，期间点按几次
- [ ] **预期**：恢复后日志仍会出现 `物理按下未触发热键`（真漏检照旧被抓），说明修复只去掉了"快速点按"这一类假阳性

> 对应更改七：用户实测发现边界问题——「测试操作 2 后用快捷键禁用按键，随后出现一条 `物理按下未触发热键：key=w`」。排查确认：探针的监视键位表每 5s 才刷新一次，而禁用热键会**立刻**清空 `ActiveHotkeys`，这 5 秒空窗里探针仍会为已注销的键建档 → 3s 后误报。已修：键位集合变更**即时**通知探针重建监视表 + 结算时复核该键是否仍在监视表。

### 功能点：禁用热键后不再误报「未触发热键」

- [x] **操作**：游戏在前台 → 按一次某键（如 `w`）并松开 → **3 秒内**用快捷键禁用热键（默认 `<^MButton`）→ **紧接着（5 秒内）再按一次同一个键** → 等 3 秒以上
- [x] **预期**：**不出现** `[WARN] [HookHealth] 物理按下未触发热键`；日志顺序应为「热键已禁用」→（无 WARN）
- [x] **操作**：在禁用状态下再随意按几次该键与其它键，等 5 秒以上
- [x] **预期**：仍然没有该 WARN（监视表在禁用瞬间就空了，不是等 5 秒定时刷新）
- [x] **操作**：重新启用热键，正常按键
- [x] **预期**：功能照常；日志里 `probe=[…]` 中该键满足 `arm ≈ cleared + miss + discard + watchDrop`，且 `miss` 不增长、`watchDrop` 可能为 1（那条被正确作废的观测）
- [ ] **操作**：（可选反证）临时把 `HotkeyService._NotifyWatchKeysChanged()` 的两处调用注释掉后重建，重复第一个操作
- [ ] **预期**：WARN 重新出现——证明修复来自"即时刷新 + 结算复核"，而不是把检测关掉

---

## 测试结果

- [x] 全部通过（含 5 项未执行的可选反证，均已在预期中标注"可选"；下述问题均已修复并复测）
- [ ] 存在问题（详见下方问题反馈）

## 问题反馈

### 问题1：诊断包脱敏摧毁日志（用户名 `1` 时 1099 行日志被替换 4037 处）
- [x] 已解决（移除 USER 全文替换；实测导出后时间戳/pid/句柄完整）

### 问题2：noActivate 热键被误判「钩子被摘除」
- [x] 已解决（为该路径补轻量观测包装；`fire` 计数恢复、无自愈误触发）

### 问题3：`inToAction` 指标在生产上无法自证正确（连报"输入交付 3.9s"假延迟）
- [x] 已解决（整项移除；快照只保留可验证的 `ctxEval` 与 `probe` 自证计数）

### 问题4：动作阶段日志插在 `Send Down/Up` 之间导致按键时序错乱（右键返回上级菜单不稳定）
- [x] 已解决（阶段日志相关改动整体回滚；该教训写入 `docs/design/input_stall_diagnosis.md`）

### 问题5：快速点按鼠标键误报「未触发热键」（竞态判据基准错误）
- [x] 已解决（基准改为「上次观测到抬起」）

### 问题6：禁用热键后 5 秒内按同一键误报「未触发热键」
- [x] 已解决（键位变更即时刷新监视表 + 结算复核该键是否仍被监视）
