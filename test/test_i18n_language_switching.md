# 测试清单：`i18n_language_switching`

> 对应更改：补全中英日三语界面翻译，修复应用语言后回退中文、动态标签重新写回中文及英文 Language 控件重叠问题。

## 测试环境

| 项目 | 信息 |
|------|------|
| AFA 版本 | `v2.0.0-alpha.1` |
| AutoHotkey 版本 | `2.0.26` |
| Windows 版本 | `Windows 10.0.26100.4351` |
| 测试日期 | `2026-08-21` |

---

## 单元测试

### 功能点：语言设置控件

- [x] **操作**：打开“其他设置 → 启动与退出”，依次选择简体中文、English、日本語。
- [x] **预期**：下拉框包含三个选项；`Language/言語/语言` 标签与下拉框之间有间距，不遮挡、不重叠。
- [x] **操作**：切换至 English，查看底部“保存并关闭、应用设置、取消”按钮。
- [x] **预期**：按钮显示为 `Save and Close`、`Apply Settings`、`Cancel`，文字完整且不溢出。

### 功能点：功能页翻译与布局

- [x] **操作**：在 English 下依次打开 Combat、Quick Actions、STRONGHOLD PROTOCOL。
- [x] **预期**：顶部标签、热键名称、提示语均为英文；绑定输入框不遮挡左侧名称。
- [x] **操作**：在日本語下重复检查通常作戦、クイック操作、堅守協定。
- [x] **预期**：顶部标签、热键名称、提示语均为日文，指定术语显示为堅守協定、統合戦略、基地、更新、凍結、準備完了。

---

## 集成测试

### 流程：应用语言并保持设置

> 涉及模块：`Config`、`I18n`、`SettingsService`、`GuiManager`

- [x] **前置**：启动最新的 `artifacts/AFA-issue288-test.exe`。
- [x] **操作**：选择 English 后点击 `Apply Settings`，切换各标签页和其他设置分类。
- [x] **预期**：界面立即保持英文，不跳回中文；标签切换不会重新出现中文。
- [x] **操作**：关闭并重新启动 AFA。
- [x] **预期**：仍使用 English，热键和其他设置值不变。
- [x] **操作**：切换到日本語并点击保存，然后重新打开设置窗口。
- [x] **预期**：仍使用日本語，托盘菜单和设置窗口语言一致。

### 流程：更新和公告窗口

> 涉及模块：`I18n`、`UpdateUI`、`ChangelogUI`

- [x] **操作**：在 English 和日本語下分别触发更新检查并打开更新公告。
- [x] **预期**：窗口标题、按钮、复选框和静态提示使用当前语言；远程公告正文保持原文。

##### 异常路径

- [x] **异常**：手工将 `Settings.ini` 的 `Language` 改为无效值后启动 → **预期**：安全回退到简体中文，不报错。

---

## 回归测试

### 功能：设置与热键

- [x] 验证：应用、保存、取消和重置按键仍正常工作。
- [x] 验证：顶部标签排序、隐藏状态和卫戍协议模式切换不受语言切换影响。
- [x] 验证：托盘菜单可正常打开设置、切换热键、重启和退出。

## 追加测试

> 对应更改：卫戍协议操作中的“升级”日语文案改为 `レベルアップ`，并构建测试 exe。

### 功能点：卫戍协议日语术语

- [x] **操作**：启动 `artifacts/AFA-issue288-japanese-levelup-test.exe`，将界面切换为日本語，打开 `堅守協定` 页面。
- [x] **预期**：升级操作显示为 `レベルアップ`，不再显示 `強化`；其他操作和输入框布局无变化。

### 功能点：测试 exe 基本启动

- [x] **操作**：双击 `artifacts/AFA-issue288-japanese-levelup-test.exe`，打开设置窗口并切换中文、English、日本語。
- [x] **预期**：程序可正常启动，设置窗口和托盘菜单可用，三种语言切换不报错。
- [x] **操作**：在 English 和日本語下分别打开 `堅守協定/Stronghold Protocol` 页面。
- [x] **预期**：English 显示 `Upgrade`，日本語显示 `レベルアップ`。
- [x] **操作**：在日本語的“起動と終了”设置中查看 `AFAと同時にアークナイツを起動` 和 `アークナイツと同時にAFAを起動`。
- [x] **预期**：两个复选框的日语文字完整显示为单行，不被上下或左右截断。

##### 异常路径

- [x] **异常**：关闭测试 exe 后再次启动 → **预期**：程序可再次启动，语言设置和热键配置保持不变。

---

## 测试结果

- [x] 全部通过
- [ ] 存在问题（详见下方问题反馈）

## 问题反馈

无
