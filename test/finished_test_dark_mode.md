# 测试清单：深色模式与系统主题跟随

## 追加测试：恢复深色绘制后的主窗口双缓冲（2026-09-08）

> 对应更改：主设置窗口启用 WS_EX_COMPOSITED，保留恢复后的主题绘制及现有切页顺序。环境：AFA v2.0.3、AHK 2.0.26、Windows 11 build 26100 x64；以下完整应用验收待测，不继承历史通过结果。

- [x] **操作**：在浅色、深色下快速切换常规作战、快捷操作、自定义按键及其他设置分类。
  - [x] **预期**：逐个控件出现的闪烁减轻，无明显拖延、残影或空白页面。
- [x] **操作**：输入未保存值，切页再返回；编辑/选择文本，操作复选框、齿轮与展开的下拉列表。
  - [x] **预期**：输入和勾选保留，焦点、选区及点击行为正常，列表完整显示。
- [x] **操作**：拖动标签顺序、显隐标签；隐藏并重开主窗口，再切换深浅主题并取消预览。
  - [x] **预期**：布局、窗口显隐和主题恢复正常，透明区域无异常背景。
- [x] **操作**：在100%和150%缩放、五种界面语言下复查上述操作，并开启真实高对比度检查可读性。
  - [x] **预期**：控件无覆盖或截断，高对比度配色正常；未测试组合保持待验。

## 追加测试：主题日志与 API 兼容性

> 补充 Theme 初始化/预览/确认/生效配色/窗口资源日志；可选 DWM 属性按版本和 HRESULT 处理，绘制故障去重延后写入。此前测试状态不变。

- [ ] **操作**：启动并切换浅色、深色、跟随系统，预览后取消，再导出日志。
  - [ ] **预期**：Theme 日志能关联已保存模式、预览模式、系统明暗、高对比度和生效结果；初始化包含 OS/AHK/位数。
- [ ] **操作**：反复移动鼠标、切页，停留不更改主题；关闭并重新打开弹窗。
  - [ ] **预期**：不因每次绘制或鼠标移动新增成功日志；窗口登记/注销可追踪，重复同类失败不刷屏。
- [ ] **操作**：在可用的 Windows 10 与 Windows 11 环境打开主窗口、公告、更新及按键编辑弹窗。
  - [ ] **预期**：旧系统跳过不支持的标题栏颜色/背景材质；无法应用的可选属性记录原因并继续使用原生外观，不中断启动或设置。未具备的系统环境记录为未测。

隔离验证已覆盖：初始化/切换日志、重复 Refresh/绘制不刷日志、失败 HRESULT 去重、22000/22621 边界判断及资源清理；实机平台为 Windows 11 build 26100 x64。版本判断模拟不等于 Windows 10/x86 实机验收。

## 追加测试：切页流程与上游 main 对齐

> 已撤掉批量暂停重绘方案；切页显隐与刷新顺序对齐上游 main，仅保留主题颜色接口。对齐不代表闪烁已消除，实际表现待复测。

- [ ] **操作**：浅色和深色下反复切换“常规作战／快捷操作”，并切换其他设置分类。
  - [ ] **预期**：显隐、数值刷新与上游 main 一致，无额外暂停重绘逻辑；记录是否仍出现空白或闪烁。
- [ ] **操作**：修改未保存设置后切页、切回，并重新打开设置窗口。
  - [ ] **预期**：未保存值、主题配色和窗口显隐正确，之前的控件修复仍保留。

## 追加测试：齿轮图标完整显示

> 调整图标字体按钮的深色绘制边距；不改变按钮尺寸或普通文字按钮。既有结果保留，本轮待手工复测。

- [ ] **操作**：深色模式打开自定义按键页，查看各行齿轮按钮，并悬停、点击打开编辑窗口。
  - [ ] **预期**：齿轮完整、居中，未裁掉左右部分；点击和悬停提示正常。
- [ ] **操作**：切换浅色／深色，在 100%、150%、200% 缩放下检查齿轮及普通文字按钮。
  - [ ] **预期**：图标完整，浅色原生绘制和普通按钮的文字边距不变。

## 追加测试：PR #356 复选框与输入框绘制

> 本轮仅修正浅色复选框视觉主题、深色勾选图形及深色 Edit 边框悬停动画冲突。既有结果保留，以下完整应用回归待验证。

- [ ] **操作**：浅色模式检查勾选／未勾选复选框，再从深色切回浅色。
  - [ ] **预期**：使用系统现代复选框样式，不退回经典方框；点击与空格键切换正常。
- [ ] **操作**：深色模式检查复选框的选中、悬停、按下、禁用状态，并在不同缩放下观察勾选图形。
  - [ ] **预期**：勾选由系统视觉主题绘制，边缘平滑、各状态可辨认，不再使用明显锯齿折线。
- [ ] **操作**：深色模式在多个按键 Edit 输入框之间来回移动鼠标、移出窗口、切换焦点，再切回浅色。
  - [ ] **预期**：先前经过的输入框边框不再延迟亮暗闪烁；光标、选区、按键录制和浅色原生样式保持正常。
- [ ] **操作**：检查只读公告的滚动条及高对比度下的复选框和输入框。
  - [ ] **预期**：滚动正常，高对比度保留系统绘制；下拉框箭头与左对齐修复仍有效。

## 追加测试：PR #357 下拉框样式

> 修复深色绘制误把 DDL 控件当成按钮的问题；既有通过记录保持不变。本轮完整界面待复测。

- [ ] **操作**：深色模式下检查主题、语言、帧率和更新渠道下拉框，展开并选择选项。
  - [ ] **预期**：选项文字左对齐，右侧显示下拉箭头；展开、选择及键盘操作正常。
- [ ] **操作**：切换浅色、深色和高对比度，并检查普通按钮。
  - [ ] **预期**：浅色／高对比度仍使用原生绘制；普通按钮样式不受这次修复影响。

> 对应更改：新增跟随系统 / 浅色 / 深色；选择立即预览，保存或应用后持久化，取消恢复。统一适配应用窗口，保留原生控件交互。

## 测试环境

| 项目 | 信息 |
|---|---|
| AFA 版本 | 初轮及两轮修复验收：`v2.0.1`；当前合并后构建：`v2.0.3` |
| AutoHotkey 版本 | 项目约定 v2.0.26 |
| Windows 版本 | 已知主机 build 26100；用户确认测试全部通过，其他测试主机版本未单独记录 |
| 测试日期 | 2026-09-05 |
| 验证状态 | 37 项手工测试全部通过（74 个勾选）；合并 develop 后 3 项结果由用户手工填写 |

## 单元测试

### 1. 入口、默认值和模式

- [x] **操作**：打开“其他 → 显示”，检查“界面主题”下拉框。
  - [x] **预期**：选项顺序为“跟随系统 / 浅色 / 深色”，与顶部标签管理器不重叠。
- [x] **操作**：退出 AFA，备份 Settings.ini；删除 `[Main] ThemeMode` 后重启。
  - [x] **预期**：选择“跟随系统”，首次窗口按 Windows 应用模式显示；旧设置保留。
- [x] **操作**：退出后将 ThemeMode 设置为非法值，重启。
  - [x] **预期**：按跟随系统显示，无错误弹窗；下次正常保存得到合法模式值。

### 2. 即时预览

- [x] **操作**：依次选择深色、浅色、跟随系统，不点击保存。
  - [x] **预期**：立即换色，无窗口重建；选择不同于初始模式时显示未保存状态，INI 不因预览被写入。
- [x] **操作**：预览期间修改其他设置、切换标签页并从托盘再次打开设置。
  - [x] **预期**：编辑值、当前主题和未保存状态保留；切页仍按原规则切换热键组，主题切换本身不切组。

### 3. 控件状态

- [x] **操作**：深色下操作按钮、复选框、下拉框及其展开列表、数值微调控件；使用 Tab、Space、Enter 和快捷键。
  - [x] **预期**：文字可读，悬停/按下/选中/焦点清晰；勾选、键盘操作及数值变化与浅色相同。
- [x] **操作**：切换更新源，检查禁用的 Token 行；查看只读公告和下载进度。
  - [x] **预期**：禁用状态可辨认，只读文本和进度信息可读；内容区域不残留白底。
- [x] **操作**：制造并解除按键冲突，在冲突存在时切换主题。
  - [x] **预期**：冲突持续显示错误色，保存按钮状态正确；解除冲突后恢复当前主题的文字色。
- [x] **操作**：深色下拖动顶部标签、切换显示/隐藏、切换其他设置分类。
  - [x] **预期**：行高亮、选中指示、眼睛图标、分割线和状态栏颜色正确，无残留高亮。

## 集成测试

### 1. 保存、取消和失败路径

- [x] **操作**：选择深色后点击应用，再重启 AFA。
  - [x] **预期**：应用后窗口保留，成功消息框也是深色；重启保持深色，INI 为 `ThemeMode=dark`。
- [x] **操作**：选择浅色后保存并关闭，再从托盘打开。
  - [x] **预期**：窗口按原流程关闭并保存；再次打开保持浅色。
- [x] **操作**：保存跟随系统，预览手动深色，再修改 Windows 应用主题并点击取消。
  - [x] **预期**：恢复的是此刻系统主题，而不是预览前的旧颜色；主题选择恢复“跟随系统”。
- [x] **操作**：预览期间点击 X 或 Alt+F4，分别检查“关闭窗口时退出”开关的两种状态。
  - [x] **预期**：关闭/退出语义保持原样，未保存主题不落盘；下次打开或重启恢复已保存模式。
- [x] **操作**：预览深色后，在按键页执行“重置按键”，再回显示页。
  - [x] **预期**：主题预览和未保存状态保留，仅按键按原规则重置。
- [x] **操作**：在隔离的测试配置中制造保存失败（例如无效游戏路径），尝试应用主题。
  - [x] **预期**：失败提示使用预览主题，窗口保留预览与未保存状态；取消能恢复原主题，其他配置未被误保存。
- [x] **操作**：在隔离测试配置中使自定义按键文件保存失败，再尝试保存不同主题。
  - [x] **预期**：主题字段保持原值，预览与未保存状态保留；自定义按键文件成功前不提交新主题。现有其他配置的保存顺序不在本次变更中调整。

### 2. 系统跟随与窗口生命周期

- [x] **操作**：保存跟随系统，在 Windows“自定义”颜色模式下只改变默认应用模式，再只改变默认 Windows 模式。
  - [x] **预期**：AFA 跟随应用模式，不跟随任务栏/开始菜单的模式；无需重启。
- [x] **操作**：分别保存手动浅色和深色，再切换系统应用主题。
  - [x] **预期**：AFA 手动模式保持不变。
- [x] **操作**：隐藏设置窗口，切换系统主题，再打开设置；打开公告或自定义按键编辑窗口后再次切换系统主题。
  - [x] **预期**：已打开和新打开的应用窗口使用同一当前主题，不丢失正在编辑的名称和坐标。
- [x] **操作**：查看更新确认、下载进度、成功/失败/确认消息框，并重复打开和关闭。
  - [x] **预期**：内容及标题栏按系统支持能力适配，无残留窗口或销毁后的句柄错误；文件选择器、托盘菜单、调试控制台保留原样。
- [x] **操作**：启用和退出 Windows 高对比度；启用期间切换 AFA 主题。
  - [x] **预期**：高对比度期间优先系统配色，退出后恢复已保存或正在预览的模式。

## 回归测试

- [x] **操作**：在五种界面语言及 100% / 150% / 200% 缩放下检查显示页、编辑窗口和消息框。
  - [x] **预期**：新文案已翻译，无截断、重叠或异常高度；语言切换重建后主题保持一致。
- [x] **操作**：反复切换主题并打开关闭弹窗至少 30 次，检查日志与任务管理器中的 GDI 对象数量。
  - [x] **预期**：无 Theme 绘制错误，无持续增长的对象数量、白块或明显闪烁。
- [x] **操作**：在真实游戏中确认原热键行为，并在设置窗口切换主题。
  - [x] **预期**：主题变更不重注册热键、不改变游戏设置、不造成卡键。
- [x] **操作**：恢复浅色并检查原有窗口、按钮、标签排序与状态栏。
  - [x] **预期**：浅色外观及相关功能保持原样。

## 自动化检查记录

- 源码契约检查：`tools/test_theme_contract.py`，只校验源码集成与结构，不执行 AHK。
- 现有检查：分层检查、事件契约检查、国际化检查。
- 纯逻辑测试：`test/scripts/theme_test.ahk`，12 个模式解析用例、规范化、零初始化副作用探针。已准备，尚未运行。
- 冒烟脚本：已加入主题 include 与零初始化副作用探针。尚未运行。
- GUI / 游戏验收：合并前测试由用户确认通过；合并后 3 项结果由用户手工填写为通过，合计 37 项；本次仅记录用户反馈，未重新启动 AFA 或执行自动化脚本。

## 问题反馈

- 用户反馈：合并前测试全部通过；其后用户已手工填入 v2.0.3 合并回归的全部通过结果。
- 已解决并通过用户回归：删除 ThemeMode 后启动未补回配置行。
- 已解决并通过用户回归：深色输入框四周出现白色边框。

## 追加测试

> 对应更改：修复删除 ThemeMode 后启动仅使用内存默认值、未补回 INI 的问题。

- [x] **操作**：退出 AFA，在 Settings.ini 的 `[Main]` 中删除 `ThemeMode` 整行，然后重新启动修复版，不点击保存或应用。
  - [x] **预期**：文件自动补回 `ThemeMode=auto`，界面默认跟随系统，原有其他配置不变。
- [x] **操作**：分别保留 `ThemeMode=light` 和 `ThemeMode=dark` 后重启。
  - [x] **预期**：原值保持不变，不被补写为 auto。
- [x] **操作**：补回完成后再次重启，或预览另一主题后取消。
  - [x] **预期**：不产生重复主题行；取消仍恢复已保存的主题。
- [x] **操作**：在隔离配置中删除主题行并使配置目录不可写，再启动。
  - [x] **预期**：继续以 auto 默认运行，日志记录回填失败，不因回填失败中断启动；恢复写入权限后下次启动补回字段。

## 追加测试：深色输入框边框

> 对应更改：修复深色模式下 Edit 输入框四周的原生白色边框。

- [x] **操作**：在深色模式打开常规作战页面，检查按键绑定输入框，再打开按键编辑窗口输入名称和坐标。
  - [x] **预期**：输入框边框为细灰线，聚焦时为蓝色，没有白圈；文字位置及控件尺寸不变。
- [x] **操作**：在输入框切换焦点、录制按键、输入、选择、复制与删除文本。
  - [x] **预期**：原生光标和选区正常，热键录制不受影响，焦点移开后恢复灰线。
- [x] **操作**：切换更新源使 Token 输入框禁用；查看并滚动只读公告。
  - [x] **预期**：禁用和只读输入框无白圈，滚动条仍可正常使用。
- [x] **操作**：反复切换深色、浅色及系统高对比度，并检查游戏帧率下拉框。
  - [x] **预期**：浅色和高对比度恢复系统原生输入框边框；下拉框外观保持原样。
- [x] **操作**：在 100%、150%、200% 缩放下复查上述边框。
  - [x] **预期**：四边完整，无白线残留，不覆盖文本或滚动条。

## 追加测试：合并 develop 至 v2.0.3

> 对应更改：本地快进合并 develop，源码版本恢复 v2.0.3；修正上游新增原子写入函数对普通对象的属性存在性判断。此前通过的测试状态保留，本轮 3 项增量回归由用户填写为通过。

- [x] **操作**：使用合并后的源码构建并启动，查看窗口标题及“关于”页。
  - [x] **预期**：显示 v2.0.3，深色模式和输入框边框修复仍保留。
- [x] **操作**：退出后删除 `[Main] ThemeMode`，重新启动；选择手动深色并应用，再重新启动。
  - [x] **预期**：缺键补回 auto；主题保存成功且重启后保持，无对象 Has 方法相关错误。
- [x] **操作**：在隔离测试配置中将一个字母热键改为大写后启动。
  - [x] **预期**：启动时规范化为小写，热键可用；取消设置不触发额外迁移。

## 追加测试：双缓冲叠层、编辑标题栏与路径滚动条（2026-09-08）

> 用户反馈：编辑按键标题栏仍白色；顶部标签管理器内容消失；路径滚动条样式异常；顶部强调线消失。隔离截图已复现前三种视觉遮挡/标题问题及滚动条经典样式，修复后截图与滚动行为检查通过；以下完整应用待验。

- [ ] **操作**：深色下打开编辑按键窗口，切换浅色/深色并取消预览，再关闭重开。
  - [ ] **预期**：标题栏和窗口随主题变化，不再被固定白色覆盖；取消、保存与拾取行为不变。
- [ ] **操作**：打开其他设置→显示，检查五行标签名称、手柄与眼睛，点击眼睛并拖动行。
  - [ ] **预期**：内容全部可见，点击命中眼睛，拖动高亮保留文字，顺序与显隐正确。
- [ ] **操作**：浅色/深色下切换顶部标签，隐藏标签或调整顺序后再切换。
  - [ ] **预期**：强调线位于当前标签下方，宽度随布局更新，不被标签点击层遮挡。
- [ ] **操作**：启动与退出页查看短路径、多行长路径和当前客户端列表，拖动滚动条/按方向键并选择复制文本。
  - [ ] **预期**：滚动条使用现代系统样式，滚动范围、末行显示及复制正常；系统滚动条保留浅色，不承诺全深色。
- [ ] **操作**：在五语言、100%/150%缩放下复查，并快速切页、显隐窗口与启用高对比度。
  - [ ] **预期**：无新增遮挡、点击错位、残影或明显延迟；高对比度可读。

## 追加测试：左侧分类强调线（2026-09-08）

> 修复双缓冲下左侧分类竖线被导航背景遮挡；隔离浅/深色七分类共14种状态已检查。

- [ ] **操作**：浅色与深色分别点击左侧七个分类，再切出其他设置页面并返回。
  - [ ] **预期**：只有当前分类左侧显示强调色竖线，竖线位置正确，点击、焦点与顶部强调线正常。
