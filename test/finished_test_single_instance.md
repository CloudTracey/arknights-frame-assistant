# 测试清单：单例识别（命名互斥体）

> 对应更改：单例识别改用命名互斥体（新增 `base/single_instance.ahk`）；托盘「重启小助手」与提权重启前释放互斥体（修复重启被拦）；`--game-autostart` 冲突时静默退出（修复随游戏自启被拦）；手动重复启动弹多语言提示

## 测试环境
| 项目 | 信息 |
|------|------|
| AFA 版本 | `v2.0.0-beta.2` |
| AutoHotkey 版本 | `v2.0.26` |
| Windows 版本 | `（以实际测试环境为准，如有必要可补录）` |
| 测试日期 | `2026-08-25` |

---

## 单元测试

### 功能点：重复手动启动提示（多语言）

- [x] **操作**：AFA 正常运行（托盘图标存在、设置窗口可打开）；再次双击启动 AFA.exe
- [x] **预期**：弹出「已有一个AFA实例正在运行，请关闭旧实例再尝试启动新实例」提示，标题「AFA已在运行」（自定义 MessageBox 样式，非系统原生弹窗）；新实例立即退出；原实例不受影响（设置窗口、托盘、热键均正常）
- [x] **操作**：设置页将语言切换为 English (en-US) 并保存，重复上述双开操作
- [x] **预期**：弹窗显示英文「An AFA instance is already running. Please close the existing instance and try again.」，标题「AFA is already running」
- [x] **操作**（可选抽查）：切换语言为繁體中文 / 日本語 / 한국어 任一，重复双开
- [x] **预期**：弹窗语言与设置页所选语言一致
- [x] **操作**：退出 AFA 后再次双击启动
- [x] **预期**：正常启动，无任何提示弹窗

---

## 集成测试

### 流程1：托盘「重启小助手」

> 涉及模块：`SingleInstance`（base）、`GuiManager` 托盘菜单（ui）

- [x] **前置**：AFA 正常运行（本次修改版）
- [x] **操作**：右键托盘图标 → 点击「重启小助手」
- [x] **预期**：不弹出「已有一个AFA实例正在运行」；AFA 正常重启（托盘图标短暂消失后重新出现；设置窗口按配置重新打开）
- [x] **操作**：重启完成后再双击 AFA.exe
- [x] **预期**：弹出「已有实例」提示（证明重启后单例仍生效、无残留双实例）

### 流程2：AFA 先行 + 游戏后启动（随游戏自动启动）

> 涉及模块：`SingleInstance`、`GameAutoStartManager`（计划任务/审核触发）、`GameMonitor`

- [x] **前置**：设置开启「随明日方舟自动启动小助手」；AFA 正常运行；此后启动游戏
- [x] **操作**：启动明日方舟
- [x] **预期**：不弹出任何「已有实例」窗口；不出现 UAC 弹窗；运行中的 AFA 保持正常（热键可用、游戏监控有效）
- [x] **操作**：退出游戏
- [x] **预期**：AFA 按设置行为正常（如开启自动退出则随游戏退出，无异常弹窗）

### 流程3：游戏先行 + AFA 自动启动（正常路径）

> 涉及模块：`SingleInstance`、`GameAutoStartManager`

- [x] **前置**：AFA 未运行；开启自动启动设置
- [x] **操作**：启动明日方舟
- [x] **预期**：AFA 被自动拉起并正常运行，无任何提示；游戏不受影响

### 流程4：首次启动提权重启

> 涉及模块：`SingleInstance`、Bootstrap 提权路径

- [x] **前置**：无 AFA 进程运行；当前用户非管理员
- [x] **操作**：启动 AFA.exe，在 UAC 提示点「是」
- [x] **预期**：提权后正常完成启动（无「已有实例」弹窗），系统中仅一个 AFA 实例

##### 异常路径

- [x] **异常**：UAC 提示时点「取消」→ **预期**：无 AFA 实例残留，再次启动正常
- [ ] **异常**（可选，难构造，可跳过）：更新器批处理等待超时（旧实例 10s 未退出仍启动新版）→ **预期**：新实例弹「已有实例」提示后退出（已知边角）

---

## 回归测试
<!-- 备注：本分组用户选择全部跳过（未执行，后续可补测） -->

### 功能：自动更新后新版本启动

- [ ] 验证：触发一次更新，替换完成后新版本正常启动、无「已有实例」弹窗（批处理等旧进程退出后才启动新版）　*（跳过：暂不验证）*

### 功能：重启后功能完整性

- [ ] 验证：重启后的 AFA 热键正常注册/触发；游戏进程监控仍然生效　*（跳过：暂不验证）*

### 功能：同版本双开感知

- [ ] 验证：重复双击仅保留一个实例（与原行为一致）　*（跳过：暂不验证）*

---

## 测试结果

- [ ] 全部通过
- [ ] 存在问题（详见下方问题反馈）

## 问题反馈

### 问题1：[问题描述]
- [ ] 已解决
