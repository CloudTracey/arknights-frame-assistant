# 测试清单：`test_win_get_client_pos_guard`

> 对应更改：为所有 `WinGetClientPos` 调用添加 try/catch 保护，当游戏窗口突然关闭时优雅降级而非抛异常崩溃

## 测试环境

| 项目 | 信息 |
|------|------|
| AFA 版本 | `v1.5.10` |
| AutoHotkey 版本 | `v2.0.26` |
| Windows 版本 | `Windows 11 25H2` |
| 测试日期 | `2026-07-26` |

---

## 单元测试

本次更改不涉及可独立测试的单一功能——所有改动均为防御性错误处理，无新增 GUI 控件或用户可见的行为变化。

---

## 集成测试

### 流程一：自动开局暂停 — 黑屏检测阶段游戏突发关闭

> 涉及模块：`game_monitor.ahk`

- [ ] **前置**：开启自动开局暂停，游戏运行中
- [ ] **操作**：进入关卡 → 黑屏出现后**立即强制关闭游戏**（Alt+F4）
- [ ] **预期**：AFA 不崩溃，定时器继续正常运行（不弹错误框）

##### 异常路径

- [ ] **异常**：游戏在 `BlackScreenPoints()` 执行瞬间被关闭 → **预期**：AFA 不崩溃，静默跳过本次检测

---

### 流程二：自动开局暂停 — Loading 识别阶段游戏突发关闭

> 涉及模块：`game_monitor.ahk`

- [ ] **前置**：开启自动开局暂停，游戏运行中，已检测到黑屏
- [ ] **操作**：黑屏后进入 Loading 识别阶段时**立即强制关闭游戏**
- [ ] **预期**：AFA 不崩溃（这是本次修复的核心场景——用户报错的原始触发路径）

##### 异常路径

- [ ] **异常**：`LoadingPosition()` 中 `WinGetClientPos` 调用时窗口已不存在 → **预期**：返回 `false`，`CheckGameStatus` 检测后提前 return，不崩溃

---

### 流程三：暂停选中/技能/撤退/视角切换 — 操作中游戏关闭

> 涉及模块：`hotkey_actions.ahk`（`ActionPauseSelect`、`ActionPauseSkill`、`ActionPauseRetreat`、`ActionSwitchView`）

- [ ] **前置**：游戏中处于关卡内，鼠标在游戏窗口内
- [ ] **操作**：执行暂停选中（或暂停技能/撤退/视角切换）热键 → 在操作瞬间强制关闭游戏
- [ ] **预期**：AFA 不崩溃，静默终止操作

##### 异常路径

- [ ] **异常**：`PauseButtonPositionLeft/Right()` 返回 `false` → **预期**：恢复 DPI 上下文后 return，不崩溃

---

### 流程四：跳过动画 — 操作中游戏关闭

> 涉及模块：`hotkey_actions.ahk`（`ActionSkip`）

- [ ] **前置**：游戏中处于有 Skip 按钮的界面（如招募动画）
- [ ] **操作**：按下跳过热键 → 在操作瞬间强制关闭游戏
- [ ] **预期**：AFA 不崩溃

##### 异常路径

- [ ] **异常**：`SkipButtonPosition()` 返回 `false` → **预期**：恢复 DPI 上下文后 return，不崩溃

---

### 流程五：基建收取 — 操作中游戏关闭

> 涉及模块：`hotkey_actions.ahk`（`ActionHarvest`）

- [ ] **前置**：游戏中处于基建界面
- [ ] **操作**：按下基建收取热键 → 在操作瞬间强制关闭游戏
- [ ] **预期**：AFA 不崩溃

##### 异常路径

- [ ] **异常**：`HarvestButtonPosition()` 返回 `false` → **预期**：恢复 DPI 上下文后 return，不崩溃

---

### 流程六：肉鸽收集藏品 — 操作中游戏关闭

> 涉及模块：`hotkey_actions.ahk`（`ActionCollectCollectibles`）

- [ ] **前置**：集成战略中处于藏品获取界面
- [ ] **操作**：按下收集热键 → 在操作瞬间强制关闭游戏
- [ ] **预期**：AFA 不崩溃

##### 异常路径

- [ ] **异常**：`CollectButtonPosition()` 返回 `false` → **预期**：恢复 DPI 上下文后 return，不崩溃

---

### 流程七：开局暂停后代理识别阶段 — 游戏中突发关闭

> 涉及模块：`hotkey_actions.ahk`（`ActionBeginPause`）

- [ ] **前置**：自动开局暂停触发，已按下 ESC 暂停
- [ ] **操作**：暂停后进入代理识别阶段（`TakeOverButtonPositions()` 调用）时强制关闭游戏
- [ ] **预期**：AFA 不崩溃，状态正确重置

##### 异常路径

- [ ] **异常**：`TakeOverButtonPositions()` 返回 `false` → **预期**：重置 `BlackScreenDetected`/`ReadyForPause`，恢复定时器频率，break 出循环，不崩溃

---

### 流程八：游戏窗口关闭后 AFA 定时器稳定性

> 涉及模块：`game_monitor.ahk`

- [ ] **前置**：AFA 运行中，游戏已启动
- [ ] **操作**：正常关闭游戏（Alt+F4 或点击 X），观察 AFA
- [ ] **预期**：
  - 若开启自动退出 → AFA 检测到游戏进程退出后自动退出
  - 若未开启自动退出 → AFA 后台保持运行，定时器不报错

> ⚠️ 以上集成测试异常路径均需在毫秒级窗口内触发，实际使用中自然验证；回归测试已全部通过。

---

## 回归测试

### 功能：自动开局暂停 — 正常流程

- [x] 验证：正常进入关卡，黑屏检测 → Loading 识别 → 自动暂停 → 代理识别 → 整条链路正常工作
- [x] 验证：非代理作战（普通关卡）自动暂停正常触发

### 功能：暂停选中/技能/撤退/视角切换 — 正常操作

- [x] 验证：关卡内使用暂停选中热键，正常触发并工作
- [x] 验证：关卡内使用暂停技能热键，正常触发并工作
- [x] 验证：关卡内使用暂停撤退热键，正常触发并工作
- [x] 验证：关卡内使用视角切换热键，正常触发并工作

### 功能：跳过动画 — 正常操作

- [x] 验证：有 Skip 按钮时按下热键，正常跳过

### 功能：基建收取 — 正常操作

- [x] 验证：基建界面按下收取热键，正常收取

### 功能：关卡界面检测 — 正常操作

- [x] 验证：`IsInLevel()` 在关卡内返回 true，关卡外返回 false（未受影响）

### 功能：自动退出 — 正常操作

- [x] 验证：开启自动退出后，正常关闭游戏 → AFA 自动退出

---

## 测试结果

- [ ] 全部通过
- [ ] 存在问题（详见下方问题反馈）

## 问题反馈

<!-- 如有问题在此记录 -->
