# 测试清单：`issue_279_wheel_hotkey_flood_beep`

> 对应更改：`#279 全局 A_HotkeyInterval := 0，永久关闭滚轮高频热键触发的 AHK 洪峰警告弹窗与系统"嘟嘟"提示音`

## 测试环境

| 项目 | 信息 |
|------|------|
| AFA 版本 | `v2.0.0-alpha.4` |
| AutoHotkey 版本 | `v2.0.26` |
| Windows 版本 | `Windows 11 25H2` |
| 测试日期 | `2026-08-22` |

---

## 单元测试

### 功能点：滚轮热键快速滚动无系统提示音

- [x] **操作**：启动 AFA，在设置中将任一常规作战/快捷操作功能绑定为滚轮键（如 WheelUp、WheelDown），保存。
- [x] **预期**：绑定无异常，无警告弹窗。
- [ ] **操作**：进入游戏，使用无级（高分辨率）滚轮快速滚动 >2 秒。（跳过：无无级滚轮，无法复现高事件速率场景）
- [ ] **预期**：全程**无系统"嘟嘟"声**、无 AHK 警告弹窗；滚轮操作正常透传/触发热键功能。（跳过：同上）
- [ ] **操作**：将 SwitchHotkey 绑定为滚轮键后，同样快速滚动。（跳过：同上，快速滚动场景依赖无级滚轮）
- [ ] **预期**：无提示音、无警告弹窗，SwitchHotkey 功能正常。（跳过：同上）

### 功能点：普通键盘热键行为不受影响

- [x] **操作**：使用普通键盘热键（如过帧三件套、退出关卡）快速连按 >20 次。
- [x] **预期**：功能与旧版一致，无警告弹窗（旧版在此场景可能弹"脚本的高频超限"警告；按 #279 决策该告警已永久关闭，不算回归）。

---

## 集成测试

### 流程：极速连打游戏按键

> 涉及模块：`main.ahk`（A_MaxHotkeysPerInterval / A_HotkeyInterval）、`hotkey_service.ahk`、`hotkey_actions.ahk`

- [x] **前置**：AFA 运行中，游戏为前台。
- [x] **操作**：按住 WASD 等游戏攻击键极速连打 3~5 秒（每秒 >100 次）。
- [x] **预期**：无警告弹窗、无提示音；按键透传与功能触发正常，无卡键。

### 流程：启动日志确认设置生效

- [x] **前置**：开启调试模式（其他设置 → 日志 → 调试模式）。
- [x] **操作**：重启 AFA 并查看调试控制台/普通日志。
- [x] **预期**：`A_HotkeyInterval=0` 在热键注册前生效，日志无异常（无配置相关报错；该赋值不产生日志，以行为验证为准）。

---

## 回归测试

### 功能：热键注册与拦截

- [x] 验证：滚轮绑定 SwitchHotkey 之外的鼠标键热键（LButton/RButton）行为不变。
- [x] 验证：键盘热键失焦悬停路径（HoverOperate）不变。

### 功能：过帧时序

- [x] 验证：16/33/166ms 过帧三件套节奏正常（不受本改动影响，仅确认无回归）。

---

## 测试结果

- [x] 全部通过（含 2 项跳过：无级滚轮相关，见单项标注）
- [ ] 存在问题（详见下方问题反馈）

## 问题反馈

### 问题1：[问题描述]
- [ ] 已解决
