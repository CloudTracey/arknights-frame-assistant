<!-- 可交由AI根据此模版生成测试清单以节约工作量 -->
# 测试清单：`issue_348_hotkey_case_normalization`

> 对应更改：`启动时将 Settings.ini 中误写的大写字母主键原子修复为小写；写回失败时运行时仍使用规范化值。`

## 测试环境

| 项目 | 信息 |
|------|------|
| AFA 版本 | `v2.0.3` |
| AutoHotkey 版本 | `v2.0.26` |
| Windows 版本 | `待确认` |
| 测试日期 | `2026-09-04` |

---

## 单元测试

### 功能点：大写字母主键规范化

- [x] **前置**：完全退出 AFA，并备份 `%AppData%\ArknightsFrameAssistant\PC\Settings.ini`。
- [x] **操作**：在 `[Hotkeys]` 中分别设置 `Retreat=A`、`GameSpeed=<^B`、`Skill=+C`，启动 AFA 后重新打开配置文件。
- [x] **预期**：三个值分别变为 `a`、`<^b`、`+c`；显式的 Ctrl/Shift 修饰符保持不变。
- [x] **操作**：在 `[Custom]` 中设置 `SwitchHotkey=Z`，重启 AFA。
- [x] **预期**：该值变为 `z`，启用/禁用热键快捷键仍可正常触发。

### 功能点：命名键与配置边界

- [x] **操作**：分别保留或设置 `Space`、`CapsLock`、`XButton1`、`WheelUp`、`F1`、`<SHIFT`，启动 AFA。
- [x] **预期**：这些命名键的大小写和内容均不变，相关热键可正常注册。
- [x] **操作**：在 `Settings.ini` 中保留一个缺失热键项、一个空热键项以及其他 section 的自定义内容。
- [x] **预期**：迁移不会补建缺失热键，不改变空值及任何非热键配置。
- [x] **操作**：在 `CustomHotkeys.json` 的 `key` 字段保留一个大写字母值并启动 AFA。
- [x] **预期**：`CustomHotkeys.json` 不被本次迁移修改。

---

## 集成测试

### 流程：启动迁移与运行时注册

> 涉及模块：`Config.LoadFromIni`、`Config.ReadHotkeyFromIni`、`HotkeyService`

- [x] **前置**：在 `[Hotkeys]` 中设置 `DispatchCenter=A`，并在互斥标签页的另一功能中保留 `a`。
- [x] **操作**：启动 AFA，进入对应游戏场景，分别短按、长按并快速连按 A。
- [x] **预期**：配置中的 `A` 已写回为 `a`；按下和松开正确配对，无按键卡住或 Shift 异常。
- [x] **操作**：打开设置窗口，修改一个字母热键并执行“应用设置”，随后重新打开 `Settings.ini`。
- [x] **预期**：GUI 可读显示、冲突检测和热键重建均正常，落盘的字母主键为小写。
- [x] **操作**：不再修改配置并再次重启 AFA。
- [x] **预期**：配置内容不再变化，日志不再出现新的大小写迁移记录。

##### 异常路径

- [x] **异常**：备份后先在 `Settings.ini` 写入大写字母主键，再将文件设为只读并启动 AFA → **预期**：AFA 不因写回失败而中止，原文件不损坏，运行时仍按小写键注册，日志包含一次规范化写入失败 Warn。
- [x] **异常**：迁移期间目标文件无法被原子替换 → **预期**：临时文件被清理，正式配置保持原样，运行时读取仍返回规范化值。

---

## 回归测试

### 功能：配置保存与历史 A/a 问题

- [x] 验证：保存、应用、取消和重置按键的原有行为保持正常。
- [x] 验证：鼠标键、滚轮、功能键和左右修饰键的显示及注册行为不变。
- [x] 验证：#240 的 A/a Down/Up 配对修复不回归，普通字母键松开后不粘滞。
- [x] 验证：除 `[Hotkeys]` 和 `[Custom] SwitchHotkey` 外，`Settings.ini` 的其他内容保持不变。

---

## 追加测试：Shift 组合键显示

> 对应更改：修复规范化后的 `+c` 在软件中显示为 `SHIFT`/`SHIFTC`，统一显示为带分隔符的 `SHIFT+C`。

### 功能点：通用与左右 Shift 显示

- [x] **操作**：将任意功能绑定为 `Shift+C`，保存并重新打开设置窗口。
- [x] **预期**：输入框显示 `SHIFT+C`，配置文件保存为 `+c`，主键 `C` 不丢失。
- [x] **操作**：分别绑定左 Shift+C、右 Shift+C。
- [x] **预期**：输入框分别显示 `LSHIFT+C`、`RSHIFT+C`。
- [x] **操作**：仅绑定 Shift、左 Shift、右 Shift。
- [x] **预期**：输入框仍分别显示 `SHIFT`、`LSHIFT`、`RSHIFT`，单修饰键绑定不受影响。

### 回归：其他修饰键与命名键

- [x] **操作**：依次绑定 `Ctrl+C`、`Alt+C`、`Ctrl+Shift+C`。
- [x] **预期**：分别显示 `CTRL+C`、`ALT+C`、`CTRL+SHIFT+C`，保存并重开后显示一致。
- [x] **操作**：检查 `CtrlBreak`、`Space`、`XButton1` 等命名键。
- [x] **预期**：命名键不被误拆为修饰键组合，显示与触发行为保持原样。

---

## 测试结果

- [x] 全部通过
- [ ] 存在问题（详见下方问题反馈）

## 问题反馈

暂无。
