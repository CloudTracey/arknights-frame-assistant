# 测试清单：`level_detector_pixel`

> 对应更改：关卡检测改为 **PixelSearch 颜色检测投票状态机**——每秒轮询对 3 个关卡内专属对象（关卡内文本 `TextInLevel`/退出按钮 `ExitButton`/暂停按钮 `PauseButton`）做颜色检测，命中 ≥2 个置位 `State.InLevel`、<2 复位。区域用相对比例定位（`LX/RX/UY/DY` = ww/wh 比例），不依赖模板尺寸（分辨率/UI 缩放鲁棒）。低分辨率（长或宽任一 <1600×900）时 `TextInLevel` 容差放宽到 20。`GuardInLevel` 读 `State.InLevel`。对象颜色：TextInLevel 白 V1、ExitButton 灰/深红/黄绿 V3、PauseButton 白/浅灰 V3。

## 测试环境

| 项目 | 信息 |
|------|------|
| AFA 版本 | `v1.6.2` |
| AutoHotkey 版本 | `v2.0.26` |
| Windows 版本 | `Windows 11 24H2` |
| 测试日期 | `2026-08-03` |

---

## 单元测试

本次更改不涉及可独立测试的单一功能（无新增 GUI 控件或配置项）。以下为机制级验证：

### 功能点：AFA 启动与模块加载

- [x] **操作**：启动 AFA
- [x] **预期**：正常启动，无语法错误弹窗（`LevelDetector` 类定义、`Objects` 数组、`_MatchObject` 加载正常）
- [x] **操作**：游戏窗口激活，观察屏幕中央 ToolTip
- [x] **预期**：显示 `AFA 关卡检测 ○关卡外 N/3` + 各对象 ✓/✗，无 Error
- [x] **操作**：查看日志 `%AppData%\ArknightsFrameAssistant\PC\logs\afa-*.log`
- [x] **预期**：无 `LevelDetector` 相关 Error/WARN；进/出关卡时日志记录"关卡状态切换"

---

## 集成测试

### 流程：进入关卡置位

> 涉及模块：`level_detector.ahk`、`hotkey_actions.ahk`

- [x] **前置**：游戏在主界面（非关卡），游戏窗口激活
- [x] **操作**：开始进入关卡
- [x] **预期**：进入关卡后约 1s 内 `InLevel` 置位（ToolTip 变 ●关卡内，日志记"进入关卡"）
- [x] **操作**：关卡内依次按 14 个常规作战热键
- [x] **预期**：全部正常触发，无拦截日志

### 流程：退出关卡复位

- [x] **前置**：已在关卡内（`InLevel=true`）
- [x] **操作**：正常通关 / 行动失败 / 中途主动退出
- [x] **预期**：退出关卡后约 1s 内 `InLevel` 复位（ToolTip 变 ○关卡外），作战热键恢复拦截

### 流程：关卡外拦截 + 原键透传

> 涉及模块：`level_detector.ahk`、`hotkey_actions.ahk`（`KeyForward`）

- [x] **前置**：游戏处于主界面 / 准备界面 / 编队界面 / 基建 / 剧情界面
- [x] **操作**：依次按 14 个常规作战热键
- [x] **预期**：全部拦截 + 原键透传（游戏收到无 AFA 时的原生按键行为），日志有对应拦截记录

##### 异常路径

- [x] **异常**：关卡外某界面右上/左上/右下存在目标颜色（白/灰）→ **预期**：需 ≥2 个对象同时误命中才误置位，单对象误命中不影响（投票机制兜底）

### 流程：其他模式关卡（退出按钮多色）

- [x] **前置**：进入退出按钮为深红/黄绿的关卡模式
- [x] **操作**：进入该模式关卡
- [x] **预期**：`ExitButton` 命中（`0xB72518`/`0xBF2719`/`0xD0CF67`/`0xD9D86B` 多色 OR 生效），`InLevel` 置位（**首次测试不通过**：模式退出按钮形状差异大，识别线 1px 太窄；已加宽到 5px，待重测）

### 流程：分辨率适配（相对比例 + PixelSearch 尺寸无关）

- [x] **前置**：游戏分辨率 2560×1440 下已确认识别正常
- [x] **操作**：改为 1920×1080（16:9）进关卡
- [x] **预期**：仍识别（相对比例区域 + PixelSearch 不依赖模板尺寸）
- [x] **操作**：改为非 16:9（如 16:10）进关卡
- [x] **预期**：尽可能识别（记录实际表现，若某对象失配记录是哪个）

### 流程：低分辨率容差放宽

- [x] **前置**：游戏分辨率切到长或宽任一 <1600×900（如 1280×720）
- [x] **操作**：进入关卡，观察 ToolTip
- [x] **预期**：`TextInLevel` 容差自动放宽到 V20，识别正常（`InLevel` 置位）

### 流程：失焦/切窗

- [x] **前置**：已在关卡内（`InLevel=true`）
- [x] **操作**：Alt-Tab 切到其他窗口，再切回游戏
- [x] **预期**：切回后约 1s 内恢复正确 `InLevel`（回前台自愈；失焦期间不轮询）

### 流程：游戏进程退出复位

- [x] **操作**：关闭游戏进程（游戏内退出或任务管理器结束）
- [x] **预期**：`LevelDetector.Poll` 检测到进程不存在 → `InLevel` 复位 `false`，AFA 不崩溃

##### 异常路径

- [x] **异常**：游戏窗口突然关闭（`SafeWinGetClientPos` 失败）→ **预期**：轮询返回，不抛 `TargetError`，不崩溃

---

## 回归测试

### 功能：自动开局暂停（黑屏 → Loading → 暂停 → 代理识别）

- [x] 验证：正常进关卡，黑屏检测 → Loading 识别 → 自动暂停 → 代理作战识别整条链路正常

### 功能：守卫拦截透传（`KeyForward`）

- [x] 验证：关卡外按热键拦截后，down 补发 + 松开 up 补发正常；多键并发状态流正确

### 功能：热键注册与重建

- [x] 验证：切换标签页（常规作战 → 其他 → 常规作战）后，14 个守卫热键与 Up 变体正常重建，无残留、无重复注册报错

### 功能：快捷操作组 / 卫戍协议组（无守卫）

- [x] 验证：基建快速收取、肉鸽收取、跳过剧情、卫戍协议界面各热键正常

### 功能：AutoExit 未应用即生效 bug 修复（此前已修复）

- [x] 验证：GUI 勾选"随游戏自动退出"未应用不退出；应用后不立即退出，游戏下次退出才触发

---

## 追加测试：issue #240 夏活小游戏 A 键透传回归

> 对应更改：`KeyForward` 将 A/a 统一为同一状态键，并使 `InterceptedKeys` 使用大小写不敏感语义，验证关卡外人工透传的 A Down 能由对应 Up 正确结束。

### 流程：关卡外 A 键的按下/松开闭环

> 涉及模块：`level_detector.ahk`、`hotkey_actions.ahk`（`GuardInLevel` / `KeyForward`）、`hotkey_control.ahk`

- [x] **前置**：使用本次更新后的 AFA；进入夏活小游戏，保持游戏窗口激活至少 2 秒；将任意常规作战功能绑定为 `A` 并应用设置。
- [x] **预期**：日志显示关卡状态为关卡外；按 A 时出现对应的“被关卡检测拦截（不在关卡界面）”记录。
- [x] **操作**：连续 10 次按住 A 约 0.5–1 秒再松开，包含一次短按（小于约 0.1 秒）。
- [x] **预期**：每次松开后角色立即停止向左移动；不会出现持续向左、需再次按键或切换窗口才能停止的现象。
- [x] **操作**：分别将 `A` 绑定为“单位撤退”“切换倍速”“按下暂停”，每种绑定重复一次按住/松开测试。
- [x] **预期**：三种功能均不在小游戏内实际执行，A 的移动和松开行为一致正常。

### 流程：方向键对照与关卡内回归

- [x] **操作**：分别将 `W`、`S`、`D` 绑定到同一常规作战功能，在小游戏中各执行一次按住/松开测试。
- [x] **预期**：四个方向键松开后都立即停止；A 不应与 W/S/D 存在差异。
- [x] **操作**：退出小游戏并进入普通作战，等待日志显示“进入关卡”后按下绑定为 A 的常规作战功能。
- [x] **预期**：功能正常执行，游戏不会收到 A 的原始移动输入；关卡内不出现“被关卡检测拦截”日志。

##### 异常路径

- [x] **异常**：从普通作战退出后立即进入小游戏并在 1 秒内按 A → **预期**：即使 `State.InLevel` 仍在等待下一轮轮询更新，也不会出现 A 松开后持续移动；如出现，记录日志中最近的“退出关卡”时间与按键时间。
---

## 测试结果

- [x] 全部通过
- [ ] 存在问题（详见下方问题反馈）

## 问题反馈

### 问题1：其他模式关卡退出按钮识别失败
- 现象：不同模式退出按钮形状差异大，原识别线位置/长度不对导致 `ExitButton` 不命中
- 修复：识别线左移到 x=136 并加长到 y43-108（区域 136-137 × 43-108）
- [x] 已解决（重测通过）
