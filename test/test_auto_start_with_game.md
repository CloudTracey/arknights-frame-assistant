# 测试清单：`test\_auto\_start\_with\_game`

> 对应更改：新增“启动明日方舟时自动启动小助手”功能，并保留原有反向启动功能。

## 测试环境

|项目|信息|
|-|-|
|AFA 版本|`v1.9.0`|
|AutoHotkey 版本|`2.0.26`|
|Windows 版本|`Windows 11 Pro for Workstations 24H2（构建 26100.4351）`|
|测试日期|`2026-08-09`|

---

## 单元测试

### 功能点：GamePath 复用与新设置默认值

- [x] **操作**：删除或备份现有设置后启动 AFA，打开“其他设置 → 启动与退出”。
- [x] **预期**：原有“游戏路径”控件仍正常显示，新选项“启动明日方舟时自动启动小助手”默认未勾选。
- [x] **操作**：游戏运行时点击“识别游戏路径”。
- [x] **预期**：`GamePath` 被填充为实际的 `Arknights.exe` 完整路径。
- [x] **操作**：分别只勾选两个自动启动选项。
- [x] **预期**：两个选项可以独立保存和取消，不会自动修改另一个选项。

### 功能点：新功能的路径校验

- [x] **操作**：勾选“启动明日方舟时自动启动小助手”，清空游戏路径并保存。
- [x] **预期**：保存被阻止，并提示必须设置游戏路径。
- [x] **操作**：将路径改为不存在的文件或文件名不是 `Arknights.exe` 的文件并保存。
- [x] **预期**：保存被阻止，并提示路径无效。

### 功能点：游戏路径规范化

- [x] **操作**：将游戏路径填写为可解析到 `Arknights.exe` 的相对路径，启用新功能并保存。
- [x] **预期**：设置中保存为规范化后的绝对路径，计划任务事件过滤器使用相同的绝对路径。
- [x] **操作**：通过规范化后的路径启动游戏。
- [x] **预期**：AFA 自动启动且只出现一个实例。

---

## 集成测试

### 流程：启用系统事件自动启动

> 涉及模块：`game\_auto\_start.ahk`、`config.ahk`、`settings/saver.ahk`、`gui.ahk`、`main.ahk`

- [x] **前置**：AFA 以管理员权限运行，已填写有效的 `Arknights.exe` 路径，AFA 当前已退出。
- [x] **操作**：勾选新选项，确认 Windows 审核提示并保存；关闭 AFA。
- [x] **预期**：设置保存成功，Windows“进程创建成功审核”启用，计划任务 `ArknightsFrameAssistant-AutoStartWithGame-<当前用户SID>` 创建成功。
- [x] **操作**：从原有游戏入口启动明日方舟。
- [x] **预期**：AFA 自动启动且只出现一个实例，不弹出设置窗口、更新公告或自动更新提示，托盘图标正常显示。
- [x] **操作**：关闭游戏。
- [x] **预期**：在“随游戏进程关闭自动退出”开启时，AFA 自动退出。
- [x] **操作**：再次启动游戏。
- [x] **预期**：AFA 可以再次被自动启动。

##### 异常路径

- [x] **异常**：启动其他程序或不同路径下的同名程序 → **预期**：AFA 不启动。
- [x] **异常**：计划任务服务不可用或 Windows 审核设置失败 → **预期**：新选项不保存，并显示明确错误。
- [x] **异常**：AFA 已经运行时启动游戏 → **预期**：不产生第二个 AFA 实例。
- [x] **异常**：关闭功能后仍有一次遗留事件触发 → **预期**：AFA 清理计划任务后立即退出，不显示托盘或设置窗口。
- [x] **异常**：游戏已运行时启动 AFA，且两个自动启动选项都开启 → **预期**：不重复启动游戏或 AFA。

### 流程：关闭和校准自动启动

> 涉及模块：`game\_auto\_start.ahk`、`config.ahk`、`settings/saver.ahk`

- [x] **前置**：新功能已启用，计划任务存在。
- [x] **操作**：取消勾选新选项并保存。
- [x] **预期**：计划任务被删除，后续启动游戏不会自动启动 AFA；进程创建成功审核保持开启。
- [x] **操作**：开启新功能后修改游戏路径，再次保存。
- [x] **预期**：计划任务事件过滤器更新为新的游戏路径。
- [x] **操作**：启用新功能后移动 AFA EXE，手动启动移动后的 AFA。
- [x] **预期**：任务动作路径被校准为新的 EXE 路径。

---

## 回归测试

- [x] 验证“启动小助手时同时启动明日方舟”仍按原有 `AutoRunGame` 设置工作。
- [x] 验证“随游戏进程关闭自动退出”仍正常工作。
- [x] 验证“启动时打开设置窗口”在手动启动时仍按设置工作。
- [x] 验证由游戏触发的静默启动不会弹出更新公告或自动更新提示。
- [x] 验证自动更新、托盘菜单和设置保存流程不受影响。

---

## 追加测试

> 对应更改：显式启用 `SeSecurityPrivilege`，改用 Windows 审核策略 API，并细分审核权限错误。

### 功能点：审核权限预检

- [x] **前置**：使用已获得“管理审核和安全日志”用户权利的管理员账户运行 AFA。
- [x] **操作**：关闭“进程创建”成功审核后，勾选“启动明日方舟时自动启动小助手”并保存。
- [x] **预期**：AFA 自动启用当前令牌的审核权限，设置保存成功，不再出现错误码 `1314`。
- [x] **操作**：保持该功能开启，重复应用设置并重启 AFA。
- [x] **预期**：已开启的审核策略被直接复用，启动校准成功，不重复报错。

### 流程：审核策略与计划任务联动

> 涉及模块：`game\_auto\_start.ahk`、`settings/saver.ahk`、`main.ahk`

- [x] **前置**：管理员账户具备审核用户权利，已设置有效的 `Arknights.exe` 路径。
- [x] **操作**：启用随游戏自动启动，退出 AFA，再启动明日方舟。
- [x] **预期**：进程创建成功审核处于启用状态，对应计划任务存在，AFA 自动启动且只有一个实例。
- [x] **操作**：关闭随游戏自动启动并保存。
- [x] **预期**：计划任务被删除，进程创建审核保持启用，后续启动游戏不再启动 AFA。

### 回归验证

- [x] 验证无效游戏路径仍会在修改审核策略前阻止保存。
- [x] 验证“启动 AFA 时自动启动游戏”和“随游戏退出自动退出”不受影响。
- [x] 验证启用失败后其他设置仍可继续编辑并再次保存。

---

## 追加测试：读取优先与按需修复

> 对应更改：审核与计划任务改为读取优先；1450 有限重试；触发启动跳过校准；启动失败改为非阻塞托盘提示。

> 本轮构建：AFA `v1.9.0`，AutoHotkey `2.0.26`，构建日期 `2026-08-09`。系统状态测试使用真实审核策略与计划任务并在结束后恢复；错误分支使用测试注入。

### 功能点：稳定状态不重写

- [x] **前置**：功能已启用，审核成功标志和计划任务均正确；记录任务文件的最后写入时间。
- [x] **操作**：手动启动并退出 AFA 两次，不修改任何设置。
- [x] **预期**：日志显示审核和任务均已匹配并跳过写入；任务文件最后写入时间不变。
- [x] **操作**：启动明日方舟，让任务以 `--game-autostart` 启动 AFA。
- [x] **预期**：日志显示触发启动跳过审核与任务校准，AFA 正常运行且只有一个实例。

### 功能点：只修复真实漂移

- [x] **操作**：保留“进程创建”失败审核标志，关闭成功审核标志后手动启动 AFA。
- [x] **预期**：AFA 只补充成功审核标志，失败审核标志仍被保留，并在设置后复查成功。
- [x] **操作**：分别篡改任务动作参数、工作目录或事件订阅，再手动启动 AFA。
- [x] **预期**：AFA 检测到任务语义漂移并修复；再次启动时不再重写。

### 功能点：瞬态与硬错误处理

- [x] **操作**：用测试注入令首次审核事务返回 1450，随后恢复正常。
- [x] **预期**：日志记录最多 3 次尝试，间隔依次为约 250ms、750ms；恢复后继续创建或校准任务。
- [ ] **操作**：用测试账户触发错误 5、1300 或 1314 后手动启动 AFA。
- [ ] **预期**：不重试、不清除配置、不删除现有任务；GUI 就绪后仅显示一次托盘通知，主界面可继续使用。
  * **未执行原因（2026-08-09）**：当前没有可用的独立 Windows 测试账户；本项保留未勾选，现阶段以错误注入和控制流检查结果代替，不视为真实账户验收。
- [ ] **操作**：在设置页显式保存并触发相同硬错误。
- [ ] **预期**：保存被阻止并显示明确错误，启用状态不持久化。

### 功能点：关闭与遗留触发

- [x] **操作**：关闭随游戏启动并保存。
- [x] **预期**：只删除 AFA 计划任务，Windows 进程创建审核保持开启。
- [x] **操作**：配置已关闭时模拟一次遗留 `--game-autostart` 触发。
- [x] **预期**：AFA 尽力删除遗留任务后立即退出，不执行完整审核校准，也不显示主界面。
- [x] **操作**：确认任务已删除后，再次勾选“随游戏启动”并保存。
- [x] **预期**：缺失任务被视为正常未注册状态并重新创建，不再出现 `0x80070002`。

### 回归验证

- [x] 验证无效游戏路径仍会在审核操作前阻止显式保存。
- [x] 验证“启动 AFA 时自动启动游戏”“随游戏退出自动退出”和单实例行为正常。
- [x] 验证更新公告、自动更新、托盘菜单和普通设置保存不受影响。

---

## 测试结果

- [x] 历史基线测试全部通过
- [ ] 本轮“读取优先与按需修复”追加测试全部通过
- [ ] 存在问题（详见下方问题反馈）

### 本轮脚本辅助验收记录（2026-08-09）

> 以下脚本位于仓库外的本地 `D:\AFA\artifacts\`，只用于复现本机 Windows 状态和错误边界；项目内未新增自动化测试框架。需要目视判断的 GUI 项仍按贡献指南保留为手工验收。

* **测试 EXE**：`D:\AFA\artifacts\task-semantic-noop-fix\AFA-v1.9.0-task-noop-fix-v2-c0d2fee.exe`
* **真实系统状态测试**：`SYSTEM_TESTS_PASS`
  * 审核漂移修复后为“成功和失败”，原失败审核标志得到保留。
  * 任务动作参数、工作目录、事件订阅漂移均被识别并修复。
  * 修复后连续启动两次均记录“计划任务已匹配，跳过重写”。
  * 关闭功能只删除 AFA 任务；遗留 `--game-autostart` 触发完成清理后退出。
  * 任务缺失后重新启用成功，`error_80070002=False`。
* **1450 注入测试**：通过。
  * `[1450, 0]`：`success=1 attempts=2 elapsedMs=250`。
  * `[1450, 1450, 0]`：`success=1 attempts=3 elapsedMs=1000`，对应约 250ms、750ms 两次退避。
* **硬错误注入测试**：错误 `5`、`1300`、`1314` 均为 `success=0 attempts=1`，确认不重试。
* **控制流检查**：启动失败不清除配置；托盘通知仅有一个延迟调用点；设置应用失败发生在配置持久化之前并阻止保存。
* **测试后恢复**：`audit_exit=0`、`task=True`、`settings=True`，审核策略、计划任务和设置文件均恢复到测试前状态。
* **证据文件**：
  * `D:\AFA\artifacts\acceptance-system-tests-20260809\system-test-results.txt`
  * `D:\AFA\artifacts\acceptance-system-tests-20260809\injection-test-results.txt`
  * `D:\AFA\artifacts\acceptance-system-tests-20260809\VERIFICATION.txt`
* **环境限制**：当前没有独立 Windows 测试账户，因此未真实触发错误 `5/1300/1314`；该场景已完成错误注入和控制流检查，但真实账户操作及预期仍保持未勾选。
* **仍需手工验收**：在设置页目视确认硬错误对话框、启用状态未持久化，以及启动失败时托盘仅提示一次、主界面仍可使用。

## 问题反馈

* `2026-08-09`：删除计划任务后再次启用时出现 `(0x80070002) 系统找不到指定的文件`。原因是 Task Scheduler COM 返回普通 `Error`，错误码只包含在 `Message` 的 HRESULT 中，原实现只读取 `Error.Number`，把“任务不存在”误判为失败。已增加 HRESULT 低位错误码解析；最小复现由 RED 变为 GREEN，等待新测试 EXE 手工确认。
* `2026-08-09`：两次手动启动均无错误，审核查询未写入，但日志连续显示“计划任务已创建或更新”，未达到稳定状态不重写。初次修正根据导出 XML 接受 SID，但用户复测仍显示 `principal` 漂移；进一步读取 COM 发现它返回短用户名 `panyitong`，而不是 XML 中的 SID。现通过 `LookupAccountNameW` 把账户名、短用户名和 SID 统一解析为 SID 后比较，并细分主体字段日志；真实任务复现由 RED 变为 GREEN，等待 v2 EXE 确认“计划任务已匹配，跳过重写”。

## 描述

本清单覆盖 Windows 进程创建事件、计划任务、GamePath 校验、双向自动启动兼容和原有启动行为回归。
