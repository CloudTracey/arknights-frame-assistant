# AFA 全量回归测试清单（阶段 1-8 + ADR 收口 + Schema 高风险）

> 汇总文件：`test/finished_test_decoupling_phase2.md` ~ `phase8.md`、`test/finished_test_adr_closure_review.md`。

> 阶段 1 为静态分层检查与基线落地，无独立手工测试清单；其验证由 `tools/layer_check.py --baseline KNOWN_VIOLATIONS` 与 CI 持续覆盖。

## 测试环境

| 项目 | 信息 |
|------|------|
| AutoHotkey 版本 | v2.0.26 |
| Windows 版本 | Windows 11 |
| 测试日期 | 2026-08-17 ~ 2026-08-18 |

## 阶段汇总

| 阶段 | 来源文件 | 状态 |
|------|----------|------|
| 阶段 2：base 层建设 | `test/finished_test_decoupling_phase2.md` | ✅ 已归档通过 |
| 阶段 3：显式启动与零顶层副作用 | `test/finished_test_decoupling_phase3.md` | ✅ 已归档通过 |
| 阶段 4：State 删除与字段收归 | `test/finished_test_decoupling_phase4.md` | ✅ 已归档通过 |
| 阶段 5：热键域重构 | `test/finished_test_decoupling_phase5.md` | ✅ 已归档通过 |
| 阶段 6：设置域重构 | `test/finished_test_decoupling_phase6.md` | ✅ 已归档通过 |
| 阶段 7：更新/公告域重构 | `test/finished_test_decoupling_phase7.md` | ✅ 已归档通过 |
| 阶段 8：UI 层收尾与硬化 | `test/finished_test_decoupling_phase8.md` | ✅ 已归档通过 |
| ADR 收口与 Schema 高风险 | `test/finished_test_adr_closure_review.md` | ✅ 已归档通过（含已知问题见文末） |

## 阶段 2：base 层建设

### 单元测试

#### 功能点：热键 Schema 与配置加载

- [x] **操作**：打开 AFA 设置窗口，切换到「常规作战」标签
- [x] **预期**：原有热键项全部存在（按下暂停、松开暂停、切换倍速、前进 16/33/166ms、一键技能/撤退、暂停技能/撤退、视角切换、开局自动暂停开关等）
- [x] **操作**：切换到「快捷操作」标签
- [x] **预期**：左键点击、放弃行动、跳过、返回上级菜单、基建快速收取、肉鸽收取道具等项存在且默认值与改动前一致
- [x] **操作**：切换到「卫戍协议」标签
- [x] **预期**：卫戍协议热键项存在且默认值与改动前一致
- [x] **操作**：在「其他设置 → 自定义」中查看 HoverOperate 等自定义项
- [x] **预期**：自定义项仍能正常显示和保存

#### 功能点：键值格式化（KeyFormat）

- [x] **操作**：在设置窗口中点击某个热键输入框，录制组合键（如 Ctrl+Shift+A）
- [x] **预期**：输入框显示可读格式（如 LCTRL+LSHIFT+A），保存后热键仍能正确触发
- [x] **操作**：录制鼠标侧键 XButton1/XButton2
- [x] **预期**：显示为「鼠标后侧键/鼠标前侧键」，保存后触发正常
- [x] **操作**：清空某个热键
- [x] **预期**：输入框为空，保存后该热键不注册

#### 功能点：托盘提示（Tray）

- [x] **操作**：在游戏运行状态下使用「启用/禁用热键」切换热键
- [x] **预期**：托盘气泡显示「热键已启用/热键已禁用」，约 3 秒后自动消失
- [x] **操作**：使用「开局自动暂停开关」热键
- [x] **预期**：托盘气泡显示「已开启/已关闭开局自动暂停」并自动消失

#### 功能点：调试控制台（Logger 事件化）

- [x] **操作**：在「其他设置 → 日志」中勾选「调试模式」并应用
- [x] **预期**：AFA 调试日志控制台正常打开，日志实时输出；不再弹出旧的「调试日志控制台已打开」MessageBox（阶段 8 会恢复为 GUI 提示）
- [x] **操作**：取消勾选「调试模式」并应用
- [x] **预期**：控制台关闭，程序不报错

---

### 集成测试

#### 流程：程序启动与 base 层 include

> 涉及模块：`main.ahk`、`src/lib/base/*`

- [x] **前置**：无游戏进程或游戏未启动均可
- [x] **操作**：启动 AFA
- [x] **预期**：程序正常启动，无 include 路径错误、无类未定义报错，托盘图标出现
- [x] **操作**：打开设置窗口
- [x] **预期**：设置窗口正常显示，四个标签页可切换

#### 流程：设置保存/应用/取消/重置

> 涉及模块：`base/config.ahk`、`base/constants.ahk`、`base/hotkey_schema.ahk`、`settings/*`

- [x] **前置**：设置窗口已打开
- [x] **操作**：修改一个热键，点击「保存」
- [x] **预期**：设置窗口关闭，热键注册生效；再次打开设置显示新值
- [x] **操作**：修改一个热键，点击「应用」
- [x] **预期**：设置不关闭，热键立即生效，内存与 INI 一致
- [x] **操作**：修改一个热键后点击「取消」
- [x] **预期**：修改被丢弃，界面恢复为已保存值
- [x] **操作**：点击「重置按键」
- [x] **预期**：所有热键恢复默认值并保存

#### 流程：更新检查与公告（VersionUtils）

> 涉及模块：`base/version_utils.ahk`、`updater/*`、`changelog/*`

- [x] **前置**：可访问更新源（GitHub 或国内源）
- [x] **操作**：在设置中点击「检查更新」
- [x] **预期**：能正确显示最新版本或「已是最新版本」，无版本比较错误
- [x] **操作**：打开更新公告窗口
- [x] **预期**：公告正文能正确反转义并展示 Markdown，无乱码

#### 流程：游戏内过帧与窗口判定（Timing/Window）

> 涉及模块：`base/timing.ahk`、`base/window.ahk`、`hotkey_actions.ahk`

- [x] **前置**：明日方舟游戏窗口已打开并进入关卡
- [x] **操作**：使用前进 16ms / 33ms / 166ms 热键
- [x] **预期**：过帧步进正常，帧数精确性与改动前一致
- [x] **操作**：在游戏窗口内使用暂停选中/技能/撤退等热键
- [x] **预期**：动作正常触发，鼠标悬停判定与窗口存在性判定正常
- [x] **操作**：游戏窗口最小化或关闭后使用热键
- [x] **预期**：不报错、不误触发

---

### 回归测试

#### 功能：原有热键注册与守卫

- [x] 验证：常规作战、快捷操作、卫戍协议三组热键仍按标签页互斥启用
- [x] 验证：切换热键总开关后，热键禁用/启用表现与改动前一致

#### 功能：GitHub Token 与配置迁移

- [x] 验证：启动时 Token 迁移/解密提示逻辑不变
- [x] 验证：旧版 INI 中缺失的自定义配置键仍能自动回填

#### 功能：静态分层检查

- [x] 验证：运行 `python3 tools/layer_check.py --baseline KNOWN_VIOLATIONS` 输出 `PASS`
- [x] 验证：`src/lib/base/` 内 grep 无 core/ui 类引用（Config 除外）

---

## 阶段 3：显式启动与零顶层副作用

### 单元测试

#### 功能点：无顶层副作用（冒烟测试）

- [x] **操作**：在 AHK 环境中运行 `test/scripts/smoke_test.ahk`
- [x] **预期**：脚本正常退出且退出码为 0；不创建 GUI、不启动定时器、不写配置文件、不弹窗
- [x] **操作**：检查 `Config.IniFile`
- [x] **预期**：冒烟测试中 `Config.IniFile` 仍为空（未被顶层 `Config.InitPath()` 提前初始化）

#### 功能点：按键录制（KeyBinder.Start）

- [x] **操作**：打开设置窗口，点击某个热键输入框开始录制
- [x] **预期**：输入框显示「请按键」，键盘/鼠标/滚轮均可录制
- [x] **操作**：录制鼠标侧键、滚轮、左右 Alt
- [x] **预期**：可正常录制且不会触发 GUI 菜单卡死
- [x] **操作**：点击设置窗口外部区域
- [x] **预期**：录制状态取消，输入框恢复原值

#### 功能点：Alt+F4 退出（GuiManager.Start）

- [x] **操作**：打开设置窗口，按 Alt+F4
- [x] **预期**：AFA 彻底退出（与「关闭窗口时退出」设置无关）

---

### 集成测试

#### 流程：程序启动（App.Bootstrap）

> 涉及模块：`main.ahk`、全部模块

- [x] **前置**：无游戏进程或游戏未启动均可
- [x] **操作**：启动 AFA
- [x] **预期**：程序正常启动，无 include 顺序错误、无类未定义报错、无重复初始化报错；托盘图标出现
- [x] **操作**：打开设置窗口
- [x] **预期**：设置窗口正常显示，四个标签页可切换
- [x] **操作**：检查日志
- [x] **预期**：启动流程按 `App.Bootstrap()` 顺序执行，无“初始化过早/过晚”相关异常

#### 流程：设置保存/应用/取消/重置

> 涉及模块：`SettingsActionsStart`、`settings/*`

- [x] **前置**：设置窗口已打开
- [x] **操作**：修改一个热键并保存
- [x] **预期**：保存成功，热键生效，再次打开显示新值
- [x] **操作**：修改一个热键并应用
- [x] **预期**：应用成功，热键立即生效
- [x] **操作**：修改一个热键后取消
- [x] **预期**：修改被丢弃
- [x] **操作**：重置按键
- [x] **预期**：所有热键恢复默认值

#### 流程：更新检查与公告（AppStartCompleted）

> 涉及模块：`updater/*`、`changelog/*`、`game_launcher.ahk`

- [x] **前置**：可访问更新源
- [x] **操作**：启动 AFA 后等待自动更新检查
- [x] **预期**：自动更新检查正常触发（`AppStartCompleted` 事件生效）
- [x] **操作**：手动点击「检查更新」
- [x] **预期**：能显示最新版本或「已是最新版本」
- [x] **操作**：打开更新公告
- [x] **预期**：公告正常显示，无乱码
- [x] **操作**：配置了随 AFA 自动启动游戏时启动 AFA
- [x] **预期**：`GameLauncher` 在 `AppStartCompleted` 后正常响应

#### 流程：游戏监控（GameMonitor 类化）

> 涉及模块：`game_monitor.ahk`、`hotkey_actions.ahk`

- [x] **前置**：明日方舟游戏窗口已打开
- [x] **操作**：启动 AFA，进入关卡
- [x] **预期**：自动开局暂停仍按原逻辑工作（黑屏检测 → Loading 识别 → 自动暂停）
- [x] **操作**：退出游戏
- [x] **预期**：若开启自动退出，AFA 在游戏退出后自动退出
- [x] **操作**：在关卡内使用过帧/暂停/技能热键
- [x] **预期**：热键动作正常，`GameMonitor` 定时器未影响过帧时序

---

### 回归测试

#### 功能：原有热键注册与守卫

- [x] 验证：三组热键仍按标签页互斥启用
- [x] 验证：热键总开关禁用/启用正常

#### 功能：配置与 Token

- [x] 验证：启动时配置加载正常，INI 未发生意外写入
- [x] 验证：GitHub Token 迁移/解密提示逻辑不变

#### 功能：静态分层检查

- [x] 验证：运行 `python3 tools/layer_check.py --baseline KNOWN_VIOLATIONS` 输出 `PASS`
- [x] 验证：`src/lib/` 内无顶层 `SetTimer`/`OnMessage`/`EventBus.Subscribe`/`#HotIf` 副作用（`main.ahk` 的 `App.Bootstrap()` 除外）

---

## 阶段 4：State 删除与字段收归

### 单元测试

#### 功能点：State 已删除

- [x] **操作**：运行 `grep -rn "\bState\." src --include="*.ahk"`
- [x] **预期**：无输出（注释除外）
- [x] **操作**：运行 `python3 tools/layer_check.py --baseline KNOWN_VIOLATIONS`
- [x] **预期**：输出 `PASS`，且基线中不再包含 `[state]` 条目

#### 功能点：帧率与点击延迟（TimingService）

- [x] **操作**：在设置中切换帧率（30/60/90/120/144/165/180/240+）并保存/应用
- [x] **预期**：游戏内过帧步进与延迟随帧率正确变化，无 `State` 相关报错
- [x] **操作**：修改「点击延迟」自定义项并保存
- [x] **预期**：暂停选中/技能/撤退等点击延迟生效

#### 功能点：关卡守卫（LevelDetector.IsInLevel）

- [x] **操作**：进入关卡后使用常规作战热键
- [x] **预期**：热键正常放行；日志中关卡状态仍能正确切换
- [x] **操作**：在非关卡界面使用常规作战热键
- [x] **预期**：被关卡守卫拦截并透传原键

#### 功能点：失焦悬停操作（HotkeyController）

- [x] **操作**：在「自定义」中关闭「游戏窗口未激活时允许鼠标悬停触发热键」并应用
- [x] **预期**：游戏失焦且鼠标悬停时键盘热键不触发
- [x] **操作**：重新开启该选项并应用
- [x] **预期**：失焦悬停时键盘热键恢复触发

#### 功能点：游戏运行记录（GameMonitor.ResetRunRecord）

- [x] **操作**：运行游戏后退出游戏，且开启自动退出
- [x] **预期**：AFA 在游戏退出后自动退出；应用设置开启自动退出时不会因历史记录立即误退出

---

### 集成测试

#### 流程：程序启动与 owner 初始化

> 涉及模块：`main.ahk`、`AppContext`、`TimingService`、`GameMonitor`

- [x] **前置**：无游戏进程或游戏未启动均可
- [x] **操作**：启动 AFA
- [x] **预期**：正常启动，无 `State` 类未定义、无 owner 初始化顺序报错
- [x] **操作**：检查日志
- [x] **预期**：启动流程正常，`AppContext`/`TimingService` 相关初始化无异常

#### 流程：自动开局暂停（ActionBeginPause 迁入 GameMonitor）

> 涉及模块：`core/monitor/game_monitor.ahk`、`hotkey_actions.ahk`

- [x] **前置**：明日方舟游戏窗口已打开
- [x] **操作**：进入关卡，等待自动开局暂停
- [x] **预期**：黑屏检测 → Loading 识别 → 自动暂停流程正常，日志不再出现 `State.` 引用
- [x] **操作**：代理指挥关卡
- [x] **预期**：识别代理指挥后自动取消暂停，行为与改动前一致

#### 流程：自动退出与游戏状态

> 涉及模块：`core/monitor/game_monitor.ahk`、`settings/saver.ahk`

- [x] **前置**：开启自动退出
- [x] **操作**：运行游戏后退出游戏
- [x] **预期**：AFA 自动退出
- [x] **操作**：在设置中开启自动退出时游戏未运行
- [x] **预期**：不会因旧运行记录立即退出

#### 流程：随游戏自动启动（AppContext）

> 涉及模块：`core/launch/app_context.ahk`、`core/launch/game_auto_start.ahk`、`log_exporter.ahk`

- [x] **前置**：已配置随游戏自动启动
- [x] **操作**：通过 `--game-autostart` 启动 AFA
- [x] **预期**：AFA 识别启动来源并跳过校准，行为与改动前一致
- [x] **操作**：生成诊断压缩包
- [x] **预期**：诊断信息中 `StartedByGameAutoStart` 值正确

#### 流程：按键绑定窗口判定（GuiWindowName 删除）

> 涉及模块：`key_bind.ahk`、`gui.ahk`

- [x] **操作**：打开设置窗口，点击热键输入框开始录制
- [x] **预期**：录制正常；切换窗口失去焦点时录制自动取消（使用 `GuiManager.MainGui.Hwnd` 判定）

---

### 回归测试

#### 功能：原有热键与设置

- [x] 验证：三组热键互斥启用、热键总开关正常
- [x] 验证：设置保存/应用/取消/重置正常
- [x] 验证：更新检查/公告正常

#### 功能：静态检查

- [x] 验证：`src/lib/` 内无顶层副作用
- [x] 验证：`test/scripts/smoke_test.ahk` include 路径可解析，运行退出码为 0

---

## 阶段 5：热键域重构

### 单元测试

#### 功能点：热键总开关与注册表变更（目标 bug 修复）

- [x] **操作**：启动 AFA，使用热键总开关禁用热键
- [x] **预期**：热键被禁用，托盘提示「热键已禁用」
- [x] **操作**：在游戏设置中修改一个按键绑定（触发注册表变更）
- [x] **预期**：热键仍保持禁用，不会被注册表变更重新启用（修复目标 bug）
- [x] **操作**：重新启用热键
- [x] **预期**：热键按当前游戏按键绑定正常重建

#### 功能点：标签页切换与热键组

- [x] **操作**：在设置窗口切换「常规作战」/「快捷操作」/「卫戍协议」标签
- [x] **预期**：热键组随标签切换正确启用；卫戍协议切换时托盘提示「已启用/已退出卫戍协议方案」
- [x] **操作**：热键禁用状态下切换标签页
- [x] **预期**：只记录当前标签，不重建热键；重新启用后按当前标签启用

#### 功能点：切换键与托盘菜单

- [x] **操作**：设置/修改「启用/禁用热键快捷键」并应用
- [x] **预期**：托盘菜单项显示对应按键文本（如 `启用/禁用热键(F1)`）
- [x] **操作**：使用切换键禁用/启用热键
- [x] **预期**：热键状态与托盘提示正确

#### 功能点：开局自动暂停开关（SettingsValueChangeRequested 临时处理器）

- [x] **操作**：使用「开局自动暂停开关」热键
- [x] **预期**：设置项正确切换，配置保存，热键重建，托盘提示「已开启/已关闭开局自动暂停」

---

### 集成测试

#### 流程：程序启动与热键域 include

> 涉及模块：`core/hotkey/game_keys.ahk`、`core/hotkey/hotkey_service.ahk`、`core/hotkey/hotkey_actions.ahk`

- [x] **前置**：无游戏进程或游戏未启动均可
- [x] **操作**：启动 AFA
- [x] **预期**：正常启动，无类未定义/回调绑定报错；热键注册正常
- [x] **操作**：运行 `test/scripts/smoke_test.ahk`
- [x] **预期**：退出码为 0

#### 流程：热键动作与守卫

> 涉及模块：`HotkeyActions`、`LevelDetector`、`GameMonitor`

- [x] **前置**：明日方舟游戏窗口已打开并进入关卡
- [x] **操作**：使用常规作战/快捷操作/卫戍协议热键
- [x] **预期**：动作正常触发，关卡守卫正常
- [x] **操作**：使用过帧 16/33/166ms 热键
- [x] **预期**：过帧时序正常

#### 流程：设置保存/应用/取消/重置

> 涉及模块：`settings/actions.ahk`、`HotkeyService`

- [x] **操作**：修改热键并保存/应用
- [x] **预期**：热键按当前标签正确重建
- [x] **操作**：重置按键
- [x] **预期**：热键恢复默认并重建

---

### 回归测试

#### 功能：原有热键行为

- [x] 验证：三组热键互斥启用、热键总开关正常
- [x] 验证：鼠标/滚轮/键盘录制正常
- [x] 验证：更新检查/公告正常
- [x] 验证：自动开局暂停与自动退出正常

#### 功能：静态检查

- [x] 验证：`grep -n "GuiManager" src/lib/core/hotkey/*.ahk` 无代码引用（注释除外）
- [x] 验证：`python3 tools/layer_check.py --baseline KNOWN_VIOLATIONS` 输出 `PASS`
- [x] 验证：基线中不再包含 `game_keys -> GuiManager`、`hotkey_service -> GuiManager` 违规

---

## 阶段 6：设置域重构

### 单元测试

#### 功能点：设置窗口按钮事件

- [x] **操作**：打开设置窗口，修改任一设置（如帧率），观察保存/应用按钮可用
- [x] **预期**：保存并关闭、应用设置按钮变为可用；点击后分别触发 `SettingsSaveRequested`/`SettingsApplyRequested`，窗口行为与之前一致（保存后关闭，应用后不关闭）
- [x] **操作**：点击取消按钮或窗口 X（`ExitOnWindowClose=0`）
- [x] **预期**：未保存修改被丢弃，控件恢复为已保存值，窗口隐藏，保存/应用按钮回到禁用态

#### 功能点：单键切换“开局自动暂停”

- [x] **操作**：在游戏中绑定“开局自动暂停”热键，按一次切换
- [x] **预期**：托盘提示“已开启/已关闭开局自动暂停”，设置窗口（若打开）中“开局自动暂停”复选框同步变化，且不会重置其他未保存的设置
- [x] **操作**：在设置窗口先修改一个未保存的**非热键设置**（如“点击延迟”），再按“开局自动暂停”热键
- [x] **预期**：未保存的“点击延迟”修改仍然保留，不会被重载整份 INI 丢弃（目标 bug 2）

#### 功能点：更新忽略/公告忽略单键写入

- [x] **操作**：更新弹窗点击“忽略此版本”
- [x] **预期**：提示“已忽略版本”，Settings.ini 的 `LastDismissedVersion` 被写入，其他未保存设置不被全量保存
- [x] **操作**：更新公告弹窗勾选“直到下次更新前不再弹出”并确定
- [x] **预期**：Settings.ini 的 `DismissedChangelogVersion` 被写入，下次启动不再弹出当前版本公告

#### 功能点：路径规范化事件

- [x] **操作**：启用“随游戏自动启动”，填写相对/非规范游戏路径后保存
- [x] **预期**：保存成功后设置窗口的“游戏路径”显示规范化绝对路径（`GamePathNormalized` 事件驱动），且配置中保存的是规范化路径
- [x] **备注**：按当前设计仅 `AutoStartWithGame` 触发规范化；`AutoRunGame` 不触发。大小写差异不影响计划任务启动，接受当前行为。

---

### 集成测试

#### 流程：保存/应用完整流程

> 涉及模块：`SettingsService`、`Config`、`HotkeyService`、`TimingService`、`LevelDetector`、`GuiManager`

- [x] **前置**：AFA 正常运行，游戏未启动或已启动均可
- [x] **操作**：修改帧率、点击延迟、失焦悬停操作、关卡守卫、热键绑定，点击“应用设置”
- [x] **预期**：设置写入 INI；热键按新绑定重建；帧率/点击延迟立即生效；失焦悬停开关立即生效；关卡守卫轮询按开关启停；GUI 脏状态清除，窗口保持打开
- [x] **操作**：再次修改设置，点击“保存并关闭”
- [x] **预期**：设置写入 INI，热键重建，窗口隐藏，保存成功提示弹出

#### 流程：取消恢复

> 涉及模块：`SettingsService`、`Config`、`GuiManager`

- [x] **前置**：打开设置窗口并修改若干项
- [x] **操作**：点击“取消”
- [x] **预期**：所有控件恢复为已保存值，运行时缓存（帧率/悬停/守卫）同步恢复，窗口隐藏

#### 流程：重置按键

> 涉及模块：`SettingsService`、`Config`、`HotkeyService`、`GuiManager`

- [x] **前置**：修改若干热键绑定
- [x] **操作**：点击“重置按键”，确认弹窗
- [x] **预期**：所有热键恢复默认并写入 INI，热键立即重建，GUI 热键控件刷新，脏状态清除
- [x] **操作**：修改一个未保存的非热键设置（如点击延迟），点击“重置按键”并确认
- [x] **预期**：只有热键/SwitchHotkey 被重置并写入；非热键未保存修改仍保留在 GUI，且仍显示“已修改”

#### 流程：保存期间按键录制中断

> 涉及模块：`SettingsService`、`KeyBinder`

- [x] **前置**：在设置窗口点击一个热键输入框进入录制状态
- [x] **操作**：不完成录制，直接点击保存/应用
- [x] **预期**：`SettingsSaveStarting` 触发 `KeyBinder.StopHook()`，不会出现卡在录制状态或异常报错

###### 异常路径

- [x] **异常**：设置冲突时保存 → **预期**：弹出按键冲突错误，保存中止，热键保持之前状态（不崩溃）
- [x] **异常**：GitHub Token 无效时保存 → **预期**：弹出 Token 验证失败确认框；选择“否”则中止保存
- [x] **异常**：游戏路径不存在时保存 → **预期**：弹出确认框；选择“否”则中止保存

---

### 回归测试

#### 功能：热键开关与切换键

- [x] 验证：保存/应用/重置后，若热键总开关为启用，热键组按当前标签页重建；切换键（SwitchHotkey）重新注册
- [x] 验证：热键总开关禁用状态下保存设置，保存后不会意外重新启用热键
- [x] 验证：切换到“其他设置”页后，之前功能标签页的热键仍然生效；切回功能页仍正常

#### 功能：托盘提示

- [x] 验证：热键启用/禁用、卫戍协议切换、开局自动暂停切换的托盘提示仍正常显示/替换

#### 功能：更新检查与下载

- [x] 验证：手动检查更新、更新弹窗、忽略版本流程不因 SettingsService 改动而异常（既有问题“未保存设置影响检查更新”已记录，阶段 7 修复）

#### 功能：更新公告

- [x] 验证：更新公告显示、忽略版本、下次启动跳过逻辑正常

---

## 阶段 7：更新/公告域重构

### 单元测试

#### 功能点：手动检查更新按钮

- [x] **操作**：打开设置窗口 → “其他设置 → 更新”，点击“手动检查更新”
- [x] **预期**：检查期间按钮变为“检查中...”并禁用；检查完成后恢复“手动检查更新”（`UpdateCheckStarted/Completed` 事件驱动）

#### 功能点：更新弹窗按钮

- [x] **操作**：手动检查发现新版本后，点击“是”
- [x] **预期**：弹窗关闭，进入下载流程（`UpdateConfirmRequested` 事件）
- [x] **操作**：再次发现新版本，点击“否”
- [x] **预期**：弹窗关闭，无额外报错（`UpdateDismissed` 已删除）
- [x] **操作**：再次发现新版本，点击“忽略此版本”
- [x] **预期**：提示“已忽略版本”，后续自动检查不再提示该版本（`UpdateIgnoreRequested` 事件）

#### 功能点：下载弹窗与进度

- [x] **操作**：确认更新后观察下载窗口
- [x] **预期**：出现“下载中”窗口，显示进度、速度、剩余时间；取消按钮可点击
- [x] **操作**：下载过程中点击“取消下载”
- [x] **预期**：按钮变为“正在取消下载...”，随后显示“下载已取消”（`UpdateDownloadCancelRequested` → `UpdateDownloadCancelled` 事件）
- [x] **操作**：下载过程中点击“手动下载”
- [x] **预期**：打开默认浏览器访问下载页面（`UpdateManualDownloadRequested` 事件；不取消当前下载）

#### 功能点：更新公告

- [x] **操作**：启动 AFA，若有未忽略公告则自动弹出更新公告窗口
- [x] **预期**：公告窗口正常显示 Markdown 内容（`ChangelogShowRequested` → `ChangelogAvailable` 事件）
- [x] **操作**：勾选“直到下次更新前不再弹出”并确定
- [x] **预期**：窗口关闭，下次启动不再弹出当前版本公告（`ChangelogDismissRequested` 事件）

---

### 集成测试

#### 流程：完整更新检查（正常路径）

> 涉及模块：`GuiManager`、`Updater`、`VersionChecker`、`ReleaseRepository`、`UpdateUI`

- [x] **前置**：网络可用，AFA 版本低于远程最新版
- [x] **操作**：手动检查更新
- [x] **预期**：`UpdateCheckStarted` 发布；检查完成后 `UpdateCheckCompleted` 发布；发现新版本时 `UpdateAvailable` 发布并由 UpdateUI 弹窗；确认后下载窗口出现，进度更新，下载完成后提示“下载完成”

#### 流程：完整更新检查（异常路径）

> 涉及模块：`Updater`、`VersionChecker`、`ReleaseRepository`、`UpdateUI`

- [x] **异常**：网络不可用/断网时手动检查 → **预期**：显示检查失败弹窗（由 `UpdateCheckCompleted` 驱动）
- [x] **异常**：GitHub 源限流且自动检查 → **预期**：自动降级国内源，不弹错误；若手动检查则显示失败/Token 引导
- [x] **异常**：下载失败重试耗尽且备选源不可用 → **预期**：显示下载失败弹窗（`UpdateDownloadFailed` 事件）

#### 流程：公告事件链

> 涉及模块：`Bootstrap`、`ChangelogChecker`、`ChangelogUI`、`SettingsService`

- [x] **前置**：`changelog.json` 存在或可联网获取
- [x] **操作**：启动 AFA，触发 `ChangelogShowRequested`
- [x] **预期**：`ChangelogChecker` 构建 body 并发布 `ChangelogAvailable`；`ChangelogUI` 弹出公告窗口
- [x] **操作**：勾选“不再弹出”并确定
- [x] **预期**：`ChangelogDismissRequested` 由 SettingsService 单键写入，不干扰其他未保存设置

---

### 回归测试

#### 功能：设置保存/应用

- [x] 验证：阶段 6 的保存/应用/取消/重置仍正常，`SettingsService` 未受更新事件化影响

#### 功能：热键与托盘

- [x] 验证：热键开关、托盘提示、切到“其他设置”不失效等阶段 5/6 修复仍正常

#### 功能：启动流程

- [x] 验证：AFA 启动不报 include 错误，公告与自动更新检查均按事件触发

---

## 阶段 8：UI 层收尾与硬化

### 单元测试

#### 功能点：设置窗口

- [x] **操作**：启动 AFA，打开设置窗口
- [x] **预期**：窗口正常显示，标签页/控件/保存/应用/取消/重置均正常（文件已迁至 `src/lib/ui/gui.ahk`）

#### 功能点：按键录制

- [x] **操作**：在设置窗口点击一个热键输入框并录制新键
- [x] **预期**：录制/取消/保存均正常（`key_bind.ahk` 已迁至 `src/lib/ui/`）

#### 功能点：更新界面

- [x] **操作**：手动检查更新，观察更新弹窗/下载弹窗
- [x] **预期**：更新弹窗与下载弹窗正常（`updater_ui.ahk` 已迁至 `src/lib/ui/`）

#### 功能点：更新公告界面

- [x] **操作**：启动 AFA 触发公告弹窗
- [x] **预期**：公告窗口正常显示与关闭（`changelog_ui.ahk` 已迁至 `src/lib/ui/`）

#### 功能点：游戏路径识别

- [x] **操作**：打开设置窗口，点击“识别游戏路径”
- [x] **预期**：游戏路径自动填入并标记为已修改（`GamePathDetected` 事件驱动）

---

### 集成测试

#### 流程：启动流程

> 涉及模块：`Bootstrap`、`src/lib/ui/**`

- [x] **前置**：AFA 可正常启动
- [x] **操作**：启动 AFA
- [x] **预期**：无 include 错误；设置窗口/公告/自动更新检查按事件正常触发

#### 流程：保存/应用/取消/重置

> 涉及模块：`GuiManager`、`SettingsService`

- [x] **操作**：修改设置并保存/应用/取消/重置
- [x] **预期**：行为与阶段 6 一致，UI 迁移无回归

#### 流程：Token 引导打开设置

> 涉及模块：`UpdateUI`、`GuiManager`

- [ ] **前置**：手动检查更新触发 Token 引导（`suggestToken`）
- [ ] **操作**：在引导确认框选择“是”
- [ ] **预期**：打开设置窗口（`SettingsShowRequested` 事件，不再直接调 `GuiManager.Show()`）
- [ ] **备注**：跳过实际验证——手动检查时设置窗口已打开，无法观察“隐藏→显示”；已通过代码审查确认不再直接调 `GuiManager.Show()`。

---

### 回归测试

- [x] 验证：热键开关、托盘提示、切到“其他设置”不失效
- [x] 验证：更新检查/下载/忽略、公告忽略单键写入
- [x] 验证：`python3 tools/layer_check.py --baseline KNOWN_VIOLATIONS` 通过且基线为 0

---

## ADR 收口与 Schema 高风险

### 单元测试

#### 功能点：启动与日志导出（log_exporter 迁移）

- [x] **操作**：正常启动 AFA（管理员），打开「设置 → 其他设置 → 关于」或托盘相关入口
- [x] **预期**：AFA 正常启动，无 include 路径错误或弹窗
- [x] **操作**：在关于页或诊断入口点击「生成日志压缩包」
- [x] **预期**：弹出保存对话框，导出的 ZIP 包含日志、脱敏后的 Settings.ini 与诊断信息

#### 功能点：手动下载更新（UpdateUI 唯一发布点）

- [x] **操作**：打开「设置 → 其他设置 → 更新」，点击「手动下载更新」
- [x] **预期**：调用 `UpdateUI.RequestManualDownload()`，浏览器打开默认下载地址页面，不报错
- [x] **操作**：手动触发一次更新下载，在「下载中」对话框点击「手动下载(&M)」
- [x] **预期**：同样打开默认下载地址页面，且不取消当前下载

#### 功能点：设置取消/重置刷新（SettingsViewRefreshRequested）

- [x] **操作**：修改任意设置项（如切换标签顺序或修改一个非热键值），不保存，点击「取消」
- [x] **预期**：界面控件立即恢复为已保存值，保存/应用按钮状态同步复位
- [x] **操作**：点击「重置按键为默认值」并确认
- [x] **预期**：热键控件恢复默认值，其他未保存的非热键设置不被意外持久化，界面刷新无残留

#### 功能点：标签页切换刷新（GuiUpdate* 内部直调）

- [x] **操作**：在设置窗口依次切换「常规作战」「快捷操作」「卫戍协议」「其他设置」标签
- [x] **预期**：每个标签页控件显示正确、无闪烁/重复刷新异常，热键冲突标红状态保持正确

#### 功能点：热键动作去重后行为不变

- [x] **操作**：进入游戏关卡，分别触发 16ms / 33ms / 166ms 过帧热键
- [x] **预期**：过帧间隔与之前一致（无一次过两帧/不过帧），日志中动作名正确
- [x] **操作**：在基建触发「快速收取」，在肉鸽触发「收集藏品」，在剧情/招募触发「跳过」
- [x] **预期**：鼠标移动到对应按钮完成点击后回到原位置，行为与之前一致
- [x] **操作**：触发「返回上级菜单」，并开启「使用返回上级菜单放弃行动」
- [x] **预期**：ESC 后按配置补发 battleLeftPopup，旧注释代码不影响行为

#### 功能点：热键总开关与切换键（孤儿订阅清理后）

- [x] **操作**：托盘菜单「启用/禁用热键」反复切换
- [x] **预期**：热键状态正常切换，托盘提示/图标更新，无报错
- [x] **操作**：使用 SwitchKey 切换热键总开关
- [x] **预期**：切换键仍可用，注册/注销正常

---

### 集成测试

#### 流程：EventBus 异常隔离

> 涉及模块：`EventBus`、`Logger`、任意订阅者

- [ ] **前置**：开启调试日志（设置中开启调试模式）
- [ ] **操作**：在代码中临时让某个事件订阅者抛异常（或通过已有异常路径触发），再发布该事件
- [ ] **预期**：`Logger.Error("EventBus", ...)` 记录异常，且同一事件的其他订阅者仍继续执行，不中断流程
- [ ] **操作**：恢复临时异常代码
- [ ] **预期**：无残留改动

> 跳过：不临时修改代码，EventBus 异常隔离项未执行。

#### 流程：InLevelChanged 订阅调试日志

> 涉及模块：`LevelDetector`、`HotkeyService`、`Logger`

- [x] **前置**：开启调试日志/调试控制台
- [x] **操作**：进入关卡和退出关卡
- [x] **预期**：日志中出现 `Hotkey` 的 `关卡状态变化：inLevel=1/0`，热键守卫判定不受影响

#### 流程：事件契约检查器与 CI

> 涉及模块：`tools/event_contract_check.py`、`.github/workflows/architecture-check.yml`

- [x] **前置**：本地有 Python 3
- [x] **操作**：在仓库根目录运行 `python3 tools/event_contract_check.py`
- [x] **预期**：输出 `PASS: event contract check passed`
- [x] **操作**：运行 `python3 tools/layer_check.py --baseline KNOWN_VIOLATIONS`
- [x] **预期**：输出 `PASS`（0/0）
- [x] **操作**：查看 CI 工作流 `.github/workflows/architecture-check.yml`
- [x] **预期**：包含 `Run event contract check` 步骤

---

### 回归测试

#### 功能：设置保存/应用/重置

- [x] 验证：保存、应用、取消、重置四个操作均正常，事件顺序无回归（`SettingsSaved/Applied/Cancelled/Reset` 仍驱动 GUI 与热键刷新）

#### 功能：更新检查与下载

- [x] 验证：手动检查更新、下载进度、取消下载、下载完成/失败提示均正常

#### 功能：启动流程

- [x] 验证：首次启动、资源提取、游戏自动启动/自动退出、公告检查均正常

#### 功能：静态检查

- [ ] 验证：涉及 include 或事件改动后，`layer_check` 与 `event_contract_check` 均通过

> 跳过：用户未手动执行；开发环境已运行通过（`PASS`/`PASS`）。

---

### 追加测试

> 对应更改：HotkeySchema 单一来源高风险收口

#### 单元测试：Schema 生成 GUI 热键行

- [x] **操作**：打开设置窗口，检查「常规作战」「快捷操作」「卫戍协议」三页的热键行
- [x] **预期**：每行显示与 Schema `displayName` 一致的名称，行顺序与之前一致，`AutoBeginPauseSwitch` 仍是复选框+输入框特殊行

#### 单元测试：ActionCallbacks 由 Schema 生成

- [ ] **操作**：启动 AFA 后触发若干热键（过帧、守卫、松开暂停、开局自动暂停开关）
- [ ] **预期**：行为与之前一致（`Guarded`/`OnUp`/`NoActivate` 标志正确生效）

> 问题：用户反馈 USleep 超时日志缺少触发位置；已改为记录 `Error.Stack` 调用栈。另确认根因为“同时按键时 PureKeyWait 饿死 USleep”，用户决定暂不修复，仅记录。

#### 集成测试：smoke_test Schema 完整性

- [x] **前置**：具备 AutoHotkey 环境
- [x] **操作**：运行 `test/scripts/smoke_test.ahk`
- [x] **预期**：脚本正常退出 0，Schema 完整性校验通过

#### 回归测试：旧别名 BeginPause

- [x] **操作**：在 Settings.ini 中保留或写入旧的 `BeginPause` 键后启动 AFA
- [x] **预期**：AFA 正常启动，忽略该旧键，不参与热键注册/默认键/GUI

---

## 已知问题汇总

- **USleep 超时日志缺少触发位置**：已解决（增加 `Error.Stack` 调用栈记录）。
- **同时按键时 PureKeyWait 饿死 USleep**：暂不修复；根因为后一个热键的 `KeyWait` 会挂起前一个热键的 `USleep`。
