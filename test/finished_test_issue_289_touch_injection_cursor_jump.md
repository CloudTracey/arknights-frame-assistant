# 测试清单：`issue_289_touch_injection_cursor_jump`

> 对应更改：`#289 启动预热统一改用屏幕坐标系取点：TouchInjector 新增 MoveFromScreen（屏幕坐标 → ScreenToClient → 游戏客户区坐标），MouseMove 在 Screen 模式下还原光标，finally 恢复 CoordMode；光标不再被挪走，预热指针位置正确`

## 测试环境

| 项目 | 信息 |
|------|------|
| AFA 版本 | `v2.0.0-alpha.4` |
| AutoHotkey 版本 | `v2.0.26` |
| Windows 版本 | `Windows 11 25H2` |
| 测试日期 | `2026-08-22` |

---

## 单元测试

### 功能点：非游戏活动窗口启动不挪光标

- [x] **前置**：明日方舟游戏正在运行；把焦点切到文件资源管理器（任意非游戏窗口为前台）。
- [x] **操作**：启动/重启 AFA。
- [x] **预期**：光标**不被挪走**，保持在原位置；无异常弹窗。

### 功能点：托盘菜单启动/重启不挪光标

- [x] **前置**：游戏运行中；通过任务栏「显示隐藏的图标」打开 AFA 托盘菜单。
- [x] **操作**：从托盘菜单启动/重启 AFA。
- [x] **预期**：光标不被挪走（托盘菜单关闭不引起漂移）。

### 功能点：游戏未运行启动

- [x] **前置**：明日方舟未运行；任意窗口为前台。
- [x] **操作**：启动 AFA。
- [x] **预期**：无异常、不弹窗、光标不变；触控预热静默跳过（游戏不存在时）。

### 功能点：游戏前台启动

- [x] **前置**：游戏运行且为前台窗口。
- [x] **操作**：启动/重启 AFA。
- [x] **预期**：光标不变；随后游戏内触控功能（暂停选人/技能/撤退）正常。

### 功能点：首次触控操作延迟

- [x] **前置**：完成一次重启后（预热已执行）。
- [x] **操作**：进入关卡，使用触控类功能（如技能、撤退）。
- [x] **预期**：首次触控操作**无旧版已知的明显延迟**（预热未失效）。

---

## 集成测试

### 流程：预热后正式触控位置正确

> 涉及模块：`core/hotkey/hotkey_actions.ahk`（HotkeyActionsStart）、`base/touch_injection.ahk`（MoveFromScreen/Move）、`core/monitor/level_detector.ahk`

- [x] **前置**：游戏运行中，按「功能点4」完成启动。
- [x] **操作**：使用暂停选人/技能/撤退触控操作（含窗口非前台时经失焦悬停路径操作）。
- [x] **预期**：触控点落在游戏客户区正确位置（与修复前一致），操作正常触发。

### 流程：多屏 / DPI 缩放场景

- [x] **前置**：多显示器（或单屏 125%/150% 缩放）。
- [x] **操作**：在副屏/缩放屏幕上运行游戏，重启 AFA。
- [x] **预期**：光标不被挪走；触控位置正确（`A_ScreenDPI` 基准下屏幕坐标经 ScreenToClient 换算无误）。

---

## 回归测试

### 功能：热键路径的 MouseGetPos Client 坐标

- [x] 验证：动作层各 `MouseGetPos &xpos, &ypos`（Client 模式）路径不受影响——坐标仍按游戏客户区解释，按钮点击位置与旧版一致。

### 功能：CoordMode 恢复

- [x] 验证：启动完成后 `CoordMode "Mouse"` 仍为 `Client`（后续热键/监控行为与旧版一致）；启动流程无异常日志。

---

## 测试结果

- [x] 全部通过
- [x] 存在问题（详见下方问题反馈）

## 问题反馈

### 问题1：[问题描述]
- [x] 已解决
